# P0.4-R4（第一期第四版修订四契约） 现行开发流程 SOP（标准作业程序）

状态：流程文档已合并更新；主副控及独立测试规则已纳入本文，尚未据此同步仓库机器约束；未定事项保留待人工裁定  
首次生成：2026-08-31；最近更新：2026-09-12（亚洲/上海时区）  
本次通知更新日期：2026-09-12  
适用范围：P0.4-R4（第一期第四版修订四契约），S1（第一阶段）～S7（第七阶段）受控开发；公共验收环境、单一原型网关、正式界面忠实度及 computer-use（计算机操作工具）真实页面操作检查规则适用于 S5（第五阶段）～S7（第七阶段）；阶段母版、UI/能力复用基线、模块独立功能优先及统一门户上下文规则自 S6（第六阶段）起适用于全部后续阶段和模块  
既有机器事实基线：`stage/s6@f444445ba2d9d2d2107f71c20a24e72f105516da`（2026-09-10 记录的第六阶段基线；本次文档合并未重新核验仓库最新状态）  
基线 tree（版本控制目录树）：`f130bd996d08e42abc65f47eb79418474be73d04`  
退役规则提交：`00ce62752cca755962f19550dc9251d041502c66`  
manifest（清单） SHA-256（安全散列算法二百五十六位）：`a4e72bec1ea3f4c51802fd7ddd95030230299d81a0a414de126f3c3be8100e98`

> 本文件是对现行机器事实和已批准人工裁定的操作化汇总，不新增门禁。若与届时最新
> clean（干净无未提交变更） Stage（阶段） 的根 `AGENTS.md`（智能体开发规则文件）、唯一 `p0-control.yml`（项目唯一控制文件）、P0.4-R4（第一期第四版修订四契约） 不可变快照或对应 Schema（结构约束）
> 冲突，以仓库机器事实为准并立即向用户报告最小冲突。

文件名按本次请求保留历史日期 2026-09-08；正文规则合并日期为 2026-09-12，既有机器事实回执仍绑定 2026-09-10，
二者不得混同，不得用文件名日期判断正文规则新旧。首次生成日期保留在上方供追溯。
S1（第一阶段）～S4（第四阶段）的历史验收环境和已完成事实不按后续新规则追溯改写。

本次重点更新：按用户要求，将 2026-09-11 两份主副控协作、独立可见测试与原开发负责人修复草案合并到本文：
主控不自行打开验收页面，只接收调度、协调和最终结果；建立两个副主控分担并发消息；
保留唯一开发负责人，允许独立可见测试任务执行真实页面测试，问题回原开发任务修复，复验沿用原测试任务，
测试通过后由原开发负责人邀请用户真实验收。独立测试接手原有页面可操作性检查，不叠加同内容的重复门禁。
两个副主控的具体分工、权限、可见性以及其他未决执行细节列在第 9.5 节，不擅自补成已定规则。
本次只更新本文；两份来源草案、仓库规则和机器配置不因此被改写，也未创建实际角色任务或操作验收环境。

此前 2026-09-10 更新已补入“两段开发与复用规则”、
`USER_REVIEW_ENTRY_LOGIN_READY`（用户验收入口登录就绪）和
`PROTOTYPE_PORTAL_CONTEXT_PARITY`（原型统一门户上下文一致性）规则；同时纳入同目录后续人工裁定文档中
用户已经确认的“最小修复、最小验证、禁止门禁偏移”边界，并明确其尚未同步为仓库机器约束。
原型并行、六个开发槽位、单一原型网关、公共真实验收窗口、正式界面忠实度和真实可操作性规则继续有效。

### 本次文档合并处理

| 旧表述或缺项 | 合并后的操作口径 |
| --- | --- |
| 一个模块只能有一个左侧任务，禁止独立测试任务 | 只限制开发生命周期任务及代码写入负责人唯一；独立可见测试任务作为明确例外，负责测试而不接管开发 |
| 原开发任务自行完成整套真实页面检查 | 原开发完成必要自测和交接，独立测试任务接手既有页面检查，问题回原开发修复，原测试任务最小复验 |
| 只有可选的临时消息协调子任务 | 纳入必须建立两个副主控的要求；具体分工和授权保留待裁定，不用临时消息整理任务冒充已落实 |
| 只限制主控打开原型，并向主控发送逐项操作记录 | 原型和正式验收页面均不由主控自行打开；主控只收调度、协调、最终结论及必要记录定位 |

### 2026-09-10 历史不一致核对结论（保留追溯，非本次重新核验）

| 原文过期或缺失点 | 当前最新规则 | 本次处理 |
| --- | --- | --- |
| 事实仍绑定 `stage/s5@d4cb0292` | 当前权威机器事实已到 `stage/s6@9c96c4e2` | 更新页首、事实回执、blob 和检查结果 |
| 写明 `FORMAL_UI_OPERABILITY_CHECK` 尚待机器同步 | 该检查以及两段复用、登录就绪、门户一致性规则已同步到根规则、控制文件、Schema 和轻量测试；第 9.3 节的后续最小修复裁定除外 | 删除过期“待同步”表述，并分别标明机器已落地项与仍待同步项 |
| 只规定各模块原型并行，没有“先母版、后并行” | 自 S6 起必须先完整批准阶段母版并激活阶段复用基线，随后才并行制作其他模块原型 | 补入两段流程、复用登记粒度和基线更新规则 |
| 只要求复用公共门户，没有可核对的门户身份和导航上下文 | 自 S6 起母版与所有后续模块必须使用同一真实 tenant/company/user、门户能力、完整导航和全局入口 | 补入门户指纹、真实服务来源、禁止全局门户 mock 及批准后纠偏规则 |
| 验收邀请只要求给出地址 | 等待 UI 或真实验收前应自动登录并停留在目标路由；确实不能自动登录时直接向用户提供临时非生产账号、密码和地址 | 补入登录就绪与凭据保密/轮换规则 |
| 未明确“原型作为正式实现合同”及模块优先策略 | formal 必须复用产品共享实现并忠实实现获批原型；模块先独立闭环，完整联动/E2E/累计验收/全局 UI 清理阶段末一次完成 | 补入 `DEFERRED` 边界和禁止重新设计/重新生成 |
| 未覆盖最新最小修复裁定 | 默认只修原问题、只验原失败和实际受影响范围，不得沿关联关系递归扩测或新增门禁 | 新增第 9.3 节，并注明人工裁定已确认、机器同步待办 |
| 旧回执宣称联动计划检查全部通过 | 核对时发现 S6 计划仍绑定旧控制文件 SHA，计划检查一度返回 `BLOCKED` | 已在不改变范围、依赖和波次的前提下刷新计划绑定；第 19 节记录发现、修复提交和复验结果 |

## 1. 权威来源与适用原则

现行事实读取顺序：

1. 届时最新 clean（干净无未提交变更） `stage/sN`（阶段编号占位符对应的阶段分支模板） 的根 `AGENTS.md`（智能体开发规则文件）。
2. 同一 Stage（阶段） tree（版本控制目录树） 的根目录唯一 `p0-control.yml`（项目唯一控制文件） 及对应 Schema（结构约束）。
3. `docs/p0/contracts/P0.4-R4/`（第一期第四版修订四不可变契约快照目录） 不可变快照及 manifest（清单） 路由结果。
4. `docs/p0/stages/stage-controller-agent-orchestration.md`（阶段总控与模块任务操作约定）、当前阶段
   `docs/p0/stages/s{stage}/business-link-plan.md/.json`（阶段业务联动规划人工与机器文件路径）、活动
   `ui-reuse-baseline.md`（界面与能力复用基线）及门户标准投影。
5. 当前阶段已登记的专项实现约束，以及用户已经明确批准且未被后续规则取代的人工裁定。

外部人工裁定目录只保存人工审阅副本，不能覆盖仓库机器事实。标记为 `DRAFT`（草案）、
`NOT_YET_EFFECTIVE`（尚未生效）、`SUPERSEDED`（已被取代） 或未批准的文件不得作为开发前置。

本次用户已明确要求把两份指定草案的规则新增/更新进入本文，因此本文采用两份材料中已明确且相互兼容的操作口径，
替换相冲突的旧文档表述；来源文件自身的“草案”状态保持原样。文档合并不等于仓库规则已经生效或实际调度已经执行。
新增角色的执行衔接与未决事项见第 9.5 节；不得用本次编辑擅自修改机器事实、扩展任务范围或启动实际开发/测试任务。

当前控制数量：

| 项目 | 数量 |
| --- | ---: |
| Stage（阶段） | 7 |
| Group（分组） | 28 |
| 业务模块 BM（业务模块） | 85 |
| 技术项 TI（技术项） | 86 |
| 公共文档 | 15 |
| 导航索引 | 18 |
| 代码模块 | 11 |
| 公共能力 | 7 |
| 端到端业务链 | 3 |

manifest（清单） 或任一 payload（清单载荷文件） 哈希不一致时立即 `BLOCKED`（已阻塞状态），不得写业务代码、创建迁移或伪造
READY（准备就绪）/测试/验收结论。

## 2. 唯一开发主流程

```
STAGE_BUSINESS_LINK_PLAN    （阶段业务联动规划）
→ PREPARE / READY    （进入只读预读准备并取得准备就绪）
→ 自 S6 起：阶段首个母版完整原型 → 用户批准 → 激活阶段 UI/能力复用基线
→ 其他模块继承基线制作完整可交互 UI 原型并报告复用项/新增项/偏离项
→ 用户逐模块明确批准完整 UI（零差异也必须人工视觉确认）
→ 原开发负责人按获批原型实现真实业务代码及直接相关必要自测
→ 正式界面忠实度核对（第五至第七阶段涉及界面的模块）
→ 精确候选部署与入口就绪，原开发任务交付完整测试流程
→ 独立可见测试任务使用 computer-use（计算机操作技能）完成真实页面检查
→ 如有失败：原开发负责人复现并修复 → 原测试任务最小复验
→ 测试通过后，原开发任务邀请用户真实功能验收
→ 用户明确验收真实功能
→ task → feat → group → stage    （任务分支依次合入功能分支、分组分支和阶段分支）
→ 自 S6 起：所有模块累计后集中完成跨模块联动、完整业务链、E2E、累计验收与全局 UI 清理
→ 用户批准进入 integration/p0 → G5    （用户批准进入第一期集成分支并执行第五门禁）
→ 用户批准进入 release/p0 → G6    （用户批准进入第一期发布分支并执行第六门禁）
→ 用户批准进入 main    （用户批准进入主分支）
```

上图为本次合并后的操作口径，新增角色的适用衔接见第 9.5 节。以下保留 2026-09-10 回执所记录的机器步骤名称；
不表示本次主副控、独立可见测试任务及消息路由已经同步到仓库根规则、机器控制文件、Schema（结构约束）或轻量测试：

```
STAGE_BUSINESS_LINK_PLAN    （阶段业务联动规划）
→ PREPARE_READY    （只读预读准备完成）
→ STAGE_MASTER_UI_APPROVAL_AND_REUSE_BASELINE    （自第六阶段起，阶段母版批准并激活复用基线）
→ PROTOTYPE_PORTAL_CONTEXT_PARITY    （自第六阶段起，原型统一门户上下文一致性）
→ INTERACTIVE_UI_PROTOTYPE    （可交互用户界面原型）
→ USER_UI_APPROVAL    （用户批准用户界面）
→ REAL_BUSINESS_CODE_AND_DIRECT_TESTS    （真实业务代码及直接相关测试）
→ FORMAL_UI_FIDELITY_CHECK    （正式界面忠实度检查，适用模块）
→ FORMAL_UI_OPERABILITY_CHECK    （正式界面真实可操作性检查，适用模块）
→ USER_REVIEW_ENTRY_LOGIN_READY    （用户验收入口登录就绪）
→ USER_REAL_FUNCTION_ACCEPTANCE    （用户真实功能验收）
→ TASK_FEAT_GROUP_STAGE    （任务、功能、分组、阶段分层合并）
→ CUMULATIVE_STAGE_LINKAGE_AND_ACCEPTANCE    （自第六阶段起，阶段末集中联动与累计验收）
→ INTEGRATION_P0_G5    （进入第一期集成分支并执行第五门禁）
→ RELEASE_P0_G6    （进入第一期发布分支并执行第六门禁）
→ MAIN    （进入主分支）
```

`FORMAL_UI_FIDELITY_CHECK`（正式界面忠实度检查）和 `FORMAL_UI_OPERABILITY_CHECK`（正式界面真实可操作性检查）
均是正式实现和用户真实验收之间的就绪要求细化，不新增分支晋级层级、独立审批流程、重复审查任务或全仓重测。
截至 2026-09-10 历史绑定提交，两项检查、阶段母版与复用基线、登录就绪和统一门户上下文规则均已进入根 `AGENTS.md`、
`p0-control.yml`、Schema（结构约束）、编排规则和轻量测试。本 SOP 仍只是人工审阅汇总，不成为第二套机器控制面。
本次改变的是既有真实页面检查的执行角色、任务可见性和反馈路由，不要求开发与测试任务各重复执行一遍同范围完整检查，
也不取消开发负责人的必要自动化测试、正式界面忠实度核对和运行就绪责任。
自 S5（第五阶段）起，具体执行节奏为：

- S5 原型展示、用户反馈、迭代和批准按模块并行；自 S6 起先完整批准本阶段首个母版并激活复用基线，
  再由其余模块按基线并行展示和迭代。每个模块即使零差异也必须单独取得用户完整视觉批准。
- 正式开发及直接相关测试继续按六个开发槽位并行；业务依赖和共享写入资源仍按登记约束执行。
- 正式实现与直接测试完成后，可按队列申请公共窗口并部署候选；涉及获批视觉合同的模块须通过正式界面忠实度核对，
  涉及界面或交互的模块还须在精确正式候选上通过 computer-use 真实页面操作检查，才能向用户发起真实功能验收请求或邀请。
  原型预览不申请或占用该窗口。
- 用户真实功能验收通过后，按任务分支、功能分支、分组分支、阶段分支依次合并，再释放窗口交给下一模块。
- 自 S6 起，模块先验收当时可独立闭环的范围；跨模块联动、完整业务链、E2E、累计验收和全局 UI 清理在全部模块
  累计进入唯一最新 clean Stage tree 后集中完成。在此之前必须标记 `DEFERRED`，不得写成 `PASS`、`VERIFIED` 或完成。

工程产物状态仍按以下顺序表达：

```
DOCUMENT_CONTRACT_MAPPED    （文档契约已映射）
→ ARTIFACT_GENERATED    （工程制品已生成）
→ IMPLEMENTED    （已经实现）
→ VERIFIED    （已经验证）
```

代码存在、截图、静态 grep（文本检索工具）、旧报告或 Agent（智能开发任务） 自报均不能让状态跳级。用户真实功能验收前
不得声明 `VERIFIED`（已经验证状态）。

## 3. SOP（标准作业程序）-01：阶段业务联动规划

### 3.1 触发时点

阶段总控必须在以下任一时点，以届时最新 clean（干净无未提交变更） `stage/sN`（阶段编号占位符对应的阶段分支模板） 为基线完成或更新
`STAGE_BUSINESS_LINK_PLAN`（阶段业务联动规划）：

- 当前阶段首个正式 task（任务分支） 创建前；
- 已在开发中的阶段开始下一滚动批次前；
- 开发中出现新的实质依赖事实时。

规划不循环复审，不追溯推翻已验收模块，不一次性规划或实现 S1（第一阶段）～S7（第七阶段） 全部模块。

### 3.2 固定产物

- 人工可读：`docs/p0/stages/s{stage}/business-link-plan.md`（阶段业务联动规划人工可读文件路径）
- 机器可读：`docs/p0/stages/s{stage}/business-link-plan.json`（阶段业务联动规划机器文件路径）
- Schema（结构约束）：`docs/governance/schema/stage-business-link-plan.schema.json`（阶段业务联动规划结构约束文件路径）
- 轻量检查：`check-stage-business-link-plan`（阶段业务联动规划轻量检查命令）

### 3.3 必须梳理的内容

1. 上一阶段已经存在、可公开消费的 provider（能力提供方）。
2. 当前阶段已批准 BM/TI（业务模块/技术项）、业务流程、事实源和业务链。
3. provider（能力提供方）、consumer（能力消费方）、查询依赖和写入依赖。
4. 删除/停用等反向引用保护。
5. 唯一事实 writer（写入者）。
6. Flyway（数据库迁移工具）、Portal（门户）、router（路由器）、权限码、错误码等共享资源窗口。
7. 无环开发波次和模块负责人。
8. 阶段结束需要执行的真实联动场景。
9. 留给下一阶段的 provider（能力提供方） 目录。

### 3.4 现行依赖类型

| 类型 | 说明 | 调度规则 |
| --- | --- | --- |
| `HARD_RUNTIME`（运行时硬依赖） | 真实运行必须依赖 provider（能力提供方） | provider（能力提供方） 不得晚于 consumer（能力消费方）；未关闭时只暂停受影响能力 |
| `HARD_ACCEPTANCE`（验收时硬依赖） | 可先实现独占部分，但完整验收必须接通 provider（能力提供方） | 只在相关功能申请真实验收前阻断 |
| `OPTIONAL_FAIL_CLOSED`（可选能力安全拒绝） | 当前可延期、可空或安全拒绝 | 不阻断无关功能，必须保留明确提示和后续接线点 |
| `REFERENCE_PROTECTION`（反向引用保护） | 删除/停用前查询下游引用 | 不得反向制造核心 provider（能力提供方） 的普遍开发前置 |
| `SHARED_FACT_WRITER`（共享事实唯一写入者） | 多模块消费同一事实 | 只允许一个公开 writer（写入者），其余模块调用公开服务 |
| `SHARED_RESOURCE_SERIAL`（共享资源串行占用） | Flyway（数据库迁移工具）、Portal（门户）、router（路由器） 等共享资源 | 阶段总控安排单一窗口，不等同业务硬依赖 |

Mock（模拟数据）、seed（种子数据）、测试夹具、硬编码、空实现和跨域直表均不能充当生产 provider（能力提供方）。

### 3.5 缺失依赖处置

缺失 provider（能力提供方） 只能选择：

1. `REUSE_EXISTING_PROVIDER`（复用现有能力提供方）：复用现有公开能力。
2. `MINIMAL_PUBLIC_SEAM`（最小公开接缝）：增加最小公开 seam/adapter（接缝/适配器），不增加新业务功能。
3. `OPTIONAL_FAIL_CLOSED`（可选能力安全拒绝）：安全拒绝或字段可空，登记后续接线点。
4. `OUT_OF_CURRENT_SCOPE`（移出当前范围）：移出当前范围并登记未来 owner（负责人）。

依赖发现不得自动新增 BM/TI（业务模块/技术项）、交互或业务功能，不得提前拉入未来阶段模块。任何范围扩大
必须重新取得用户明确批准。

S5（第五阶段）2026-09-05 的执行补充：公共依赖只补解除当前阻断的最小公开能力；某个读取接口或公共路由已合入阶段，
不等于消费者接线和整项依赖都已关闭。依赖状态须精确到已交付部分、未接通部分及阻断动作；列表、详情、草稿等
不依赖缺失能力的功能可继续开发，不把局部硬阻断扩大到整个模块。延期能力保留在计划中，不因延期被自动删除或扩成当前必做。

### 3.6 强链路局部模式

S4（第四阶段） 库存、S5（第五阶段） 交易、S6（第六阶段） 结算默认可以只对受影响业务链启用纵向链优先：

```
上一阶段公开 provider    （上一阶段公开能力提供方）
→ 当前阶段核心写入
→ 当前阶段查询/引用保护
→ 当前阶段真实联动验证
```

其他阶段只有出现真实强依赖且经用户或阶段总控裁定时才启用。未受影响模块继续按波次
并行，不得因一个模块 BLOCKED（已阻塞） 而停止整个阶段。

## 4. SOP（标准作业程序）-02：任务调度与唯一 Owner（唯一负责人）

### 4.1 阶段总控职责

阶段总控只负责：

- 联动规划和波次调度；
- 自 S6（第六阶段）起选择首个母版模块，在母版批准后建立/维护阶段 UI 与能力复用基线，并维护唯一门户上下文及标准投影；
- 创建并维护可见模块任务；
- 分配共享资源、Flyway（数据库迁移工具） 和验收环境；
- 裁定精确跨模块阻塞；
- 执行 task（任务分支）→feat（功能分支）→group（分组）→stage（阶段） 分层合并；
- 阶段冻结、统一验证、公共验收窗口交接和资源停止。

阶段总控即主控，不得直接实现业务前端、后端、数据库或 API（应用程序接口），只接收调度信息、协调请求和最终结果。
允许的内容包括最终界面批准、真实功能验收结论、精确跨模块阻塞、共享资源占用/释放、合并、冻结和资源停止事件。
开发和测试的逐步操作、普通缺陷讨论、排查日志、修复尝试及每轮进度不主动回灌主控；确需主控协调的真实阻塞仍须精确上报。

主控不自行打开、刷新、点击或测试原型及正式验收页面，也不得以查看进度、确认页面可用或检查修复为由代为操作。
需要打开或展示验收页面时，主控调度对应开发任务执行；开发任务负责原型展示、验收准备和修复复现。
测试任务按第 4.6、9.4 节自身测试权限打开对应正式验收页面，不受“主控只调度开发任务开启页面”的表述误限。
原型批准和最终真实功能验收仍由原开发任务直接向用户请求，主控不代替请求或作出用户裁定。

### 4.2 唯一可见开发任务与独立可见测试任务

- 每个 BM/TI（业务模块/技术项） 创建一个用户可在左侧 Git（分布式版本控制系统）项目“项目”下直接看到、打开和继续
  对话的独立模块开发生命周期任务；该项目对应 `D:\工作\项目`（项目主仓库目录）。
- 同一模块从交互原型、用户界面批准、正式开发、必要自测、反馈修复、用户真实验收到合并完成，始终保留原来唯一的
  左侧可见开发生命周期任务和代码写入负责人；不得按原型、正式开发或每轮修复另建第二个开发任务。
- 独立可见测试任务是本次明确允许的任务类型：用户能在左侧直接看到、打开和继续对话，不以开发任务内部隐藏子任务替代。
  名称应清楚标识阶段、模块和测试职责，并关联原开发任务；它不是第二开发负责人，不修改业务代码，不代替开发任务合并。
- 同一模块发生问题或需要复验时，沿用原测试任务和原开发任务，不为每轮测试、失败或修复新建任务。跨模块测试人员数量和分配方式见第 9.5 节。
- 开发任务可在内部使用子 Agent（智能开发任务），但子 Agent（智能开发任务）只是实现助手，不成为第二 owner（负责人），不跨模块
  写入，不绕过用户批准。

左侧项目归属与磁盘工作树位置是两个概念。任务显示在“项目”下，隔离工作树仍可按第 11.1 节
模板存放；不因原型切换为正式工作树而另建第二个可见开发任务。独立测试任务不因可见而取得第二份业务写入权限。

### 4.3 并发上限

- 左侧可见模块开发槽位：6 个。
- 全项目最多 6 个并发写入 Agent（智能开发任务）。
- 每个 Group（分组） 最多 2 个写入 Agent（智能开发任务）。
- 同一模块最多 1 个写入 owner（负责人）。
- 同一共享资源和 Flyway（数据库迁移工具） 窗口同时最多 1 个 owner（负责人）。

槽位空闲不代表可以绕过 provider（能力提供方）、UI（用户界面） 批准或共享资源窗口提前开发。

六个槽位用于模块开发，不代表六套验收环境或六个并发真实验收窗口。原型与正式开发的并行方式
见第 6.4 节，真实验收排队见第 10 节。

独立测试任务和两个副主控不增加业务代码写入负责人，也不按开发写入槽位计算；它们仍受运行资源、会话和验收窗口约束。
测试经正式页面产生验收数据的操作权不等于代码写入权，更不等于拥有独立并发验收环境。

### 4.4 角色使用

`p0-control.yml`（项目唯一控制文件） 仍登记 A（公共能力与契约角色）、B（后端与数据库角色）、C（前端与交互角色）、D（测试与验收角色）、Group Coordinator（分组协调员）、Stage Controller（阶段总控）、Integration Controller（集成总控）、
Release Gate（发布门禁）。具体任务角色必须来自当前 Group（分组）/技术任务登记；A/B/C/D（四类执行角色） 不能
被理解为同一模块的四个并行写入 owner（负责人）。

上述为既有机器角色登记；本次新增的副主控组织方式、独立可见测试任务及其路由尚未通过本文编辑同步为机器角色配置。

### 4.5 两个副主控与消息分流

本次纳入的明确要求：主控必须建立两个副主控，分担并发状态下开发任务向主控提交的调度信息与协调请求，
并对最终结果进行归并，缓解主控上下文压力。这一职责要求不能被旧的“收到两条消息才可临时开只读协调子任务”替代。
两个副主控的具体分工、是否左侧独立可见、生命周期及可自主决定的事项尚未裁定，不能自行授予合并、冻结、
停止资源、业务写入、用户验收裁定或其他超出协调职责的权限。执行衔接见第 9.5 节。

消息按以下边界处理：

- 开发任务直接向测试任务交付完整测试流程；测试任务向原开发任务反馈问题，开发任务修复后通知原测试任务复验。
- 测试任务向人工报告测试结论、发现的问题和未覆盖项，不把测试通过写成用户验收通过；最终真实验收邀请仍由原开发任务发起。
- 普通缺陷、复现、修复过程和测试步骤留在原开发/测试任务，不经主控转发，也不由副主控原样转发给主控。
- 需要调度或跨模块协调时，只报告来源、精确问题、影响范围、所需协调、候选版本和资源状态；副主控汇总去重后供主控处理。
  最终汇报只带结论与必要记录定位，不附完整日志、逐步操作流水或全部差异。
- “每条消息是否必须先经过指定副主控、哪些情况可直达主控”尚待裁定；不得因此隐藏或拖延真实阻塞，也不得自行新建审批链。

旧的按来源使用内部只读消息整理助手，仅作为历史已有能力保留说明；它不是两个副主控已经建立的证明，
也不能代替独立可见开发任务或独立可见测试任务。

### 4.6 页面开启与操作权限

- 主控没有自行打开验收页面的权限；需要页面准备或展示时调度原开发任务执行。
- 开发任务和测试任务各有打开一个对应验收页面的权利：开发负责准备、展示、问题复现和修复；测试负责按流程操作、验证和反馈。
- 页面开启权不等于并发写入权。不得抢占对方浏览器会话、登录身份、数据或操作中的页面；同一公共环境的写入操作按窗口顺序交接。
- 测试过程中固定正式候选版本，开发不得未经交接覆盖正在测试的候选；复现、重新部署和必要复验先协调操作窗口。
- “一个页面”按标签页还是浏览器会话计数、跨页操作/弹窗/下载的具体口径、额外页面的授权方式及副主控能否开页面，
  保留待人工裁定；不得自行推定无限开页或赋予副主控页面操作权。

## 5. SOP（标准作业程序）-03：PREPARE（只读预读准备） / READY（准备就绪）

PREPARE（只读预读准备） 只能进行只读核对。

### 5.1 工作现场

核对：

- 当前 Stage（阶段）、Group（分组）、BM/TI（业务模块/技术项） 和角色；
- 工作树、分支、HEAD（当前提交头）、tree（版本控制目录树）、clean（干净无未提交变更） 状态；
- formal（正式开发） 基线是否为调度时最新 clean（干净无未提交变更） `stage/sN`（阶段编号占位符对应的阶段分支模板） fixed point（固定基点）；
- 是否存在同模块第二 owner（负责人）；
- business-link plan（业务联动规划） 的波次、依赖、唯一 writer（写入者） 和共享资源窗口；
- manifest（清单）、控制文件和当前任务的允许范围。

正式 task（任务分支） 必须从调度时最新 clean（干净无未提交变更） Stage（阶段） fixed point（固定基点） 创建。禁止把 Stage（阶段） 反向
merge（合并）/rebase/cherry-pick（合并/变基/摘取提交） 到旧 task（任务分支）；旧 task（任务分支） 也不能作为新 formal（正式开发） 基线。

### 5.2 文档预读

普通 BM（业务模块） task（任务分支） 按 manifest（清单） 路由至少读取：

- 快照 manifest（清单）；
- source（来源） retrieval（检索路由） manifest（清单）；
- control（控制配置） 中的公共文档；
- 快照计划；
- 当前业务域业务索引和技术索引；
- 当前 BM（业务模块） 业务文档；
- 当前 TI（技术项） 技术切片；
- business-link plan（业务联动规划） 已声明的直接上下游和边界文件。

当前普通任务幂等键：

```
stage/group/module/role/manifestHash    （阶段、分组、模块、角色和清单哈希组成的幂等键）
```

跨阶段技术续作必须存在 `p0-control.yml`（项目唯一控制文件） 中 exact（精确登记） technical-task（技术续作任务） registration（注册记录），并严格匹配
taskId（任务标识）、registration（注册记录） id（标识）、执行 Stage（阶段）、Group（分组）、角色和分支；不得借注册身份扩大业务范围。

### 5.3 PREPARE（只读预读准备） 结果

- `READY`（准备就绪）：事实、范围、依赖、分支、owner（负责人） 和共享资源均满足，可进入下一步。
- `BLOCKED`（已阻塞状态）：精确写明最小阻塞、受影响模块/能力、责任方和解阻条件。

`BLOCKED`（已阻塞状态） 只暂停受影响模块或能力，不默认停止无依赖模块。BLOCKED（已阻塞） 状态下禁止写业务
代码、创建迁移、占用 formal（正式开发） 共享资源或伪造测试/验收结论。

## 6. SOP（标准作业程序）-04：UI（用户界面） 原型与用户批准

### 6.1 适用条件

存在可交互 UI（用户界面） 或新增交互决策时，必须先制作原型。纯后端/技术续作不得为满足形式而虚构
UI（用户界面）；是否为零 UI（用户界面） 必须由任务范围或用户裁定明确。

### 6.2 原型要求

- 分支：`prototype/p0/s{stage}/{scope}`（第一期阶段交互原型分支模板）。
- 使用真实前端技术栈、完整公共 PortalShell/workspace（门户外壳/工作区）和仅用于原型的 Mock（模拟数据）。
- 顶栏、导航、标签页、身份区、菜单加载和正式路由承载结构直接复用累计阶段公共实现；模块只实现业务内容区，
  不复制、裁剪或重制私有门户壳，不伪造登录身份或用免登录页面绕过公共壳层。
- 自 S6（第六阶段）起，门户身份、能力和完整导航由真实 `auth-center`、`system-center`、`portal-center` 提供；
  原型 Mock（模拟数据）只能位于 `MODULE_BUSINESS_CONTENT_ONLY`（仅模块业务内容）边界。
- 必须可在本地浏览器运行。
- V2/V3（第二版/第三版） 在同一 prototype（交互原型） 分支迭代。
- 模块任务自行构建、更新、打开稳定预览地址，并在自己的可见任务中直接向用户请求 UI（用户界面） 人工验收。
- 用户明确批准后记录获批 prototype（交互原型） commit/tree（提交/目录树）。

prototype（交互原型） 只用于 UI（用户界面） 决策，禁止 merge（合并） 或 cherry-pick（摘取提交） 到 formal（正式开发） task（任务分支）、Feat（功能分支）、Group（分组） 或 Stage（阶段）。
formal（正式开发） ancestry（祖先链） 必须从最新 clean（干净无未提交变更） Stage（阶段） 重新建立。

### 6.3 已退役 UX（用户体验） 概念

当前不存在已退役的全局 UX（用户体验） baseline（基线）、参考页注册表或 `UXChangeRequest`（用户体验变更申请） 机器门禁。
自 S6（第六阶段）起使用的阶段 UI/能力复用基线是获批母版形成的操作性复用目录和视觉合同，不是恢复旧全局 UX 门禁，
也不新增独立审批任务、机器回执或证据子提交。页面布局、样式和复用方式仍以用户对母版及每个模块完整原型的明确批准为准。

完整复用公共门户是现有公共代码的承载要求，不恢复全局 UX（用户体验）基线、参考页注册表或额外机器回执。
模块业务内容的交互方案仍由用户在该模块原型中直接裁决。获批原型明确指定的真实参考页属于该模块视觉合同，
正式实现须遵守；这不等同于恢复已经退役的全局参考页注册表。

### 6.4 原型并行与模块独立批准

- `UI_PROTOTYPE_REVIEW_PARALLEL`（原型评审并行）：S5（第五阶段）多个模块可直接并行；自 S6（第六阶段）起，
  必须先完成本阶段首个母版的完整用户批准并激活阶段复用基线，之后其他模块才可同时展示、接收反馈、迭代和请求批准。
  基线激活后，一个模块等待用户意见不得阻塞其他模块。
- `MODULE_UI_APPROVAL_INDEPENDENT`（模块界面批准独立生效）：本模块获批后，按
  `FORMAL_DEVELOPMENT_AFTER_MODULE_UI_APPROVAL`（模块界面获批后开始正式开发）进入正式准备与实现，
  不等待其他模块裁决；仍须满足本模块的准备结果、依赖和共享资源条件。
- `FORMAL_DEVELOPMENT_PARALLEL`（正式开发并行）：遵守第 4.3 节上限及每模块唯一负责人。
- 自 S5（第五阶段）起执行 `STAGE_PROTOTYPE_GATEWAY_MULTI_ROUTE`（阶段单一原型网关多路预览）：
  一个轻量网关、一个对外监听端口，为每个模块提供独立稳定主机名或路由，并隔离构建产物和页面状态。
  一个模块更新产物不得覆盖另一个模块；不得为原型默认创建多套完整前端、后端和数据库服务栈。
- `PROTOTYPE_PREVIEW_DOES_NOT_OCCUPY_REAL_ACCEPTANCE_WINDOW`（原型预览不占用真实验收窗口）：
  原型示例数据和轻量交互只支持界面决策，不是生产能力提供方，也不构成真实功能验收结果。
- S5（第五阶段）当前计划登记原型网关对外端口为 `13500`（第五阶段原型预览端口），真实验收前端端口为
  `13600`（第五阶段公共真实验收端口）；后续阶段以各自计划登记为准，不把这两个数值固定为全项目规则。

### 6.5 原型批准同时确定正式视觉合同

`MODULE_UI_APPROVAL_INDEPENDENT`（模块界面批准独立生效）批准的是获批 prototype commit/tree（原型提交及目录树）
表达的页面结构、关键尺寸和交互状态。正式前端不能把该批准解释为可以另行设计或任意重写界面。
对应机器规则为 `APPROVED_PROTOTYPE_COMMIT_TREE_IS_FORMAL_VISUAL_SOURCE`（获批原型提交及目录树是正式界面的视觉来源）。

模块负责人在原可见任务中，把原型页面结构、尺寸与状态逐项映射到正式组件和真实 API（应用程序接口）。
原型分支仍不得合入或摘取进正式代码历史；视觉忠实还原和版本历史隔离须同时满足。原型是正式实现合同，
不是完成后仅供参考、再由 Agent 重新设计或重新生成页面的草图。

### 6.6 阶段母版与 UI/能力复用基线（自 S6 起）

本规则适用于 S6（第六阶段）及所有后续阶段和模块，也约束对应阶段总控；不追溯改写已封板的 S1（第一阶段）～S5（第五阶段）。

1. 本阶段继承上一阶段已批准的公共壳、主题、布局和可复用资产。阶段总控先选择首个母版模块，执行
   `FIRST_STAGE_MODULE_FULL_UI_APPROVAL_BEFORE_LATER_PROTOTYPES`（首个阶段模块完整界面批准先于后续原型）；
   母版完整原型获得用户批准前，不派发其余模块的 UI 原型制作。
2. 母版批准后，阶段总控按 `docs/p0/stages/ui-reuse-baseline-template.md` 生成并激活
   `docs/p0/stages/s{stage}/ui-reuse-baseline.md`，登记布局、主题、样式、关键尺寸、组件和能力复用目录。
3. 后续模块必须先读取当前阶段基线，在完整原型中报告全部“复用项 / 新增项 / 偏离项”；没有差异时也必须明确报告“零差异”。
   无论是否存在差异，用户都必须查看完整页面并明确批准，之后才可进入 formal（正式开发）。默认只制作一个继承母版的方案；
   仅在用户要求或确有设计分歧时提供 A/B/C（多方案）备选。
4. 复用登记粒度固定为：公共能力逐能力登记；共享领域能力和组合 UI（组合用户界面）逐可复用组件登记；
   基础控件按组件族登记；模块私有小组件不登记；标准业务按钮继承所属组件族，不逐按钮登记。
5. 资产状态只允许 `REUSE_READY`、`REFERENCE_REUSE`、`BUILD_ONCE`、`REUSE_AFTER_PROVIDER`、
   `MINIMAL_PUBLIC_SEAM`、`MODULE_PRIVATE`、`NOT_APPLICABLE`、`FAIL_CLOSED`。
   只有用户已经批准、具备清晰 owner/provider（负责人/能力提供方）并确认跨模块复用的新增共享能力或组合组件才加入活动基线；
   模块私有项不得伪装成共享资产，也不得为了登记而提前抽象。

### 6.7 统一门户上下文、指纹核对与批准后纠偏（自 S6 起）

阶段总控必须在母版请求完整 UI 批准前声明本阶段唯一受控门户上下文，并在阶段复用基线中记录不含凭据的指纹。
母版和所有后续模块均执行 `PROTOTYPE_PORTAL_CONTEXT_PARITY`：使用相同的 tenant（租户）、company（公司）、user（用户）、
门户能力、完整 navigation module/group/menu（导航模块/分组/菜单）的 code/title/icon/route（编码/标题/图标/路由）及全局门户入口。
未读数量和当前打开页签是易变状态，不纳入一致性判断。

如用哈希表达完整导航，阶段总控必须同时写明唯一规范化方法，包括顶层包装、字段及字段顺序、数组顺序、空值/缺失字段、
UTF-8（统一字符编码）、字符转义和空白处理，并保存不含身份或 secret（秘密凭据）的机器可读标准投影或共享校验器；
测试必须实际重算数量和哈希。正式目录变化时，模块只报告字段级差异并停止审批，由阶段总控确认来源后统一刷新基线，
不得由各模块自行修改目录或分别接受不同指纹。

门户运行时必须来自真实 `auth-center`、`system-center`、`portal-center`。模块 Agent（智能开发任务）和原型网关不得通过
CDP（浏览器调试协议）/浏览器请求拦截、Service Worker（服务工作线程）、本地 fixture（夹具）或硬编码伪造、替换或裁剪
`AUTH_SESSION`、`PORTAL_BOOTSTRAP`、`PORTAL_NAVIGATION`，也不得把单模块导航冒充完整门户进行 UI 批准。
模块在报告“等待 UI 验收”前必须完成登录、逐项比对并向用户报告；任一不一致不得请求或登记完整 UI 批准。

若模块已经获批或进入 formal 后才发现门户上下文不一致，保留模块业务内容选择和已有工作，不回滚、不重建 formal ancestry（正式祖先链）；
允许门户无关的后端、API、直接测试和模块私有内容继续，暂停共享 PortalShell/router/menu（门户外壳/路由/菜单）接入、
正式 UI 忠实度结论、真实功能验收、分层合并和 `VERIFIED`。同一可见模块任务必须使用真实统一门户重新展示完整页面，
取得用户整页复批后才恢复被暂停环节。

## 7. SOP（标准作业程序）-05：正式开发

需要交互批准的模块，在本模块 UI（用户界面）批准后，由同一个可见模块任务独立进入 formal（正式开发）阶段；
不等待其他模块批准，也不等待公共真实验收窗口空闲。纯后端或技术续作按其已批准范围执行：

1. 阶段总控从届时最新 clean（干净无未提交变更） `stage/sN`（阶段编号占位符对应的阶段分支模板） fixed point（固定基点） 创建正式 task（任务分支）/worktree（工作树）。
2. 再次执行 formal（正式开发） PREPARE（只读预读准备），确认 Stage（阶段） 没有漂移出当前任务依赖。
3. 按当前 BM/TI（业务模块/技术项）契约实现真实前端、后端、数据库和 API（应用程序接口）；存在获批原型时，
   复用产品树中已登记的公共能力、共享领域能力、组合 UI 和基础控件族，把原型 Mock 替换为真实 API、权限、状态和持久化，
   同时完成页面结构、关键尺寸和交互状态到正式组件与真实接口的逐项映射；不得重新设计或重新生成页面。
4. 仅修改本模块和获批准的最小公开 seam/adapter（接缝/适配器）。
5. 共享资源、Flyway（数据库迁移工具）、Portal（门户）/router（路由器） 等必须取得阶段总控单 writer（写入者） 窗口。
6. 开发中发现新实质依赖时，只更新 business-link plan（业务联动规划） 和受影响波次，不重写已验收 task（任务分支），
   不修改已发布迁移。

模块开发期间只运行本模块及直接上下游的必要测试，不反复运行全仓门禁。

自 S6（第六阶段）起，各模块优先完成当时可独立闭环的功能、直接相关测试、正式 UI 忠实度和用户真实功能验收。
跨模块联动、完整业务链、E2E（端到端测试）、累计验收与全局 UI 清理统一保持 `DEFERRED`，待所有模块累计进入唯一最新 clean
Stage tree 后由阶段总控集中完成；business-link plan（业务联动规划）声明的硬依赖仍必须在对应阻断时点关闭。

正式实现与直接测试完成后，按第 10 节申请公共资源窗口并部署候选；涉及界面的模块在向用户发起真实验收请求或邀请前，
完成第 7.1 节核对。忠实度检查不新增资源窗口申请前置；需要正式环境时可在所分配窗口内完成检查。
单测试文件夹具专项按第 12.3 节执行，不为该类修复增加界面原型或重复业务验收。

### 7.1 正式界面忠实度检查

本节依据 2026-09-07 21:40 纳入第五阶段的规则，机器适用范围为 S5（第五阶段）～S7（第七阶段）。
`FORMAL_UI_FIDELITY_CHECK`（正式界面忠实度检查）必须在模块向用户发起真实功能验收请求或邀请前执行一次。
纯后端、无界面的技术任务不为此虚构视觉合同。

1. 固定桌面 viewport（浏览器可视区域），逐屏通过人工或截图比对正式页面与获批原型提交及目录树。
2. 只检查本模块视觉合同、新增改动和受影响状态，不重新启动全量审查。
3. 原型明确指定真实参考页时，复用该参考页已采用的 Element Plus（界面组件库）表格、可筛选选择器、抽屉以及尺寸密度模式。
   确需偏离时，先在原模块任务中取得用户重新批准；不得用主观“相似”替代已批准的要求。
4. 最小直接视觉回归覆盖下表六类内容，不能只断言总宽度或文案存在。
5. 映射和核对记录保存在当前模块任务，绑定实际获批原型与正式候选；不新增独立审批流程、审查任务或证据子提交。

| 机器检查项 | 人工核对内容 |
| --- | --- |
| `WORKSPACE_ROW_MODEL`（工作区行模型） | 页面各行的组织、布局关系与原型一致 |
| `COLUMN_WIDTHS`（逐列宽度） | 逐列核对表格列宽，不能仅比较总宽度 |
| `ROW_HEIGHT`（行高） | 表格及相关行的高度一致 |
| `FONT_SIZE`（字号） | 关键内容和操作文字的字号一致 |
| `POPOVER_AND_DRAWER_DIMENSIONS`（浮层和抽屉尺寸） | 浮层、抽屉的尺寸与指定参考模式一致 |
| `CRITICAL_INTERACTION_STATES`（关键交互状态） | 核对本模块获批的关键操作状态与变化 |

用户在真实验收中判定复刻失败时，原 UI（用户界面）批准仅继续对原型有效，formal（正式实现）立即回到修复态。
修复并通过忠实度检查前，不得再次请求业务验收、分层合并或标记 `VERIFIED`（已经验证状态）。
截图在此用于限定范围的视觉对比，不能替代功能、权限、安全、数据库或业务链的真实运行验证。

### 7.2 正式界面真实可操作性检查

本节依据 2026-09-08 18:40:52 +08:00 用户人工裁定新增，适用于 S5（第五阶段）～S7（第七阶段）所有包含正式页面或用户交互的模块。
S1（第一阶段）～S4（第四阶段）已经完成的历史模块不追溯重验；后续若恢复开发或修复影响真实操作的缺陷，则仅对新增改动、
未关闭缺陷和受影响业务链执行本节检查。

`FORMAL_UI_OPERABILITY_CHECK`（正式界面真实可操作性检查）必须在模块负责人向用户发起真实功能验收请求或邀请前完成。
该检查与第 7.1 节并列：第 7.1 节确认正式页面“忠实复刻”，本节确认正式页面“真实可用”。两项均通过后，模块才具备邀请用户验收的条件；
两项均不能替代用户最终人工验收。

按本次合并口径，本节由独立可见测试任务接手执行，原开发负责人交付精确候选和完整测试流程并负责问题修复。
适用衔接见第 9.5 节：不要求原开发任务先把本节全部操作跑一遍，再让测试任务重复跑一遍。
开发侧已有必要自动化测试、正式界面忠实度核对、启动登录及目标路由可达检查继续保留。
测试依据是已批准需求、完整获批原型和开发提供的测试流程三者，不得只按开发列出的按钮清单判断覆盖完整性。

执行要求如下：

1. 在阶段总控分配的唯一公共真实验收环境中，使用与用户相同的正式地址、Portal（门户）、登录身份、权限和数据范围，运行精确的
   formal task commit/tree（正式任务提交及目录树）。禁止在原型页、standalone（独立模拟页面）、测试组件或无法定位版本的“最新代码”上代替执行。
2. 测试任务必须使用 computer-use（计算机操作技能），通过真实浏览器中的页面可见控件逐项操作冻结验收范围内的功能。
   直接调用 API、执行 SQL（结构化查询语言）、修改 DOM（文档对象模型）、在控制台调用内部函数或仅查看截图，均不能代替页面操作；
   API、日志和数据库只用于核对页面动作产生的真实副作用。
3. 按模块实际范围至少检查：菜单与正式路由、列表加载、筛选与重置、分页与滚动、详情、创建、编辑、选择控件、保存草稿、提交、
   状态推进、允许的反向动作、刷新后状态与数据持久化。某项不在冻结范围时明确记为“不适用”，不得无说明跳过。
4. 导出、下载、打印、上传、附件、导入或后台任务属于冻结范围时，必须验证最终结果，而不是只看按钮提示或任务受理响应：
   导出/下载须生成本次新文件并实际打开核对内容；打印须打开真实预览并核对业务字段；上传/附件须完成选择、提交、绑定和刷新回显；
   后台任务须确认终态、结果文件或明确失败信息。
5. 本模块已批准、当时可独立闭环的验收范围包含跨页面或跨模块业务链且依赖已具备时，须从上游页面真实操作进入下游页面并继续执行，
   直到状态、数量、金额、库存、附件、审计或其他冻结业务结果回写上游并在刷新后仍正确；仅能跳转或生成下游单据不代表链路完成。
   阶段末集中执行的完整业务链仍明确延期，不得只因模块存在关联就提前扩大本轮范围。
6. 同时核对关键失败路径：无权限、缺少权威数据、版本冲突、非法状态、必填缺失和 fail-closed（失败时安全拒绝）。失败应显示明确、
   可理解且与真实原因一致的提示，不得无响应、静默成功、错误启用按钮或把 `UNAVAILABLE`（能力不可用）伪装为完成。
7. 测试操作使用专用验收数据。允许为验证生命周期通过正式页面执行保存、提交、取消、确认、出入库等已授权写入动作；
   既有正式接口辅助准备不替代本节要求的页面操作。
   记录业务对象编号及操作前后状态；不得直写业务表、复用无法辨认来源的脏数据或破坏其他模块正在使用的验收样本。
8. 完整检查记录保存在原独立可见测试任务，至少包含：阶段固定基点、正式候选 commit/tree（提交/目录树）、环境地址、角色与数据范围（不记录凭据）、
   验收数据编号、逐项操作步骤、可见结果、业务副作用、刷新结果、文件或任务结果、未检查项及原因。
   原开发任务接收问题和最终结论并引用记录；主控只接收必要摘要与定位。不得每轮另建测试任务、重复审查任务或证据子提交。

通过标准：冻结验收范围内的全部适用操作均可由页面完成，控件可见、可点击、可选择、可录入，提交结果与真实后端、数据库及下游事实一致，
刷新后不丢失，文件与跨页结果真实存在；不存在仍返回 `UNAVAILABLE`、仅有占位入口、禁用按钮、内容裁切导致不可操作、前后端运行版本不一致，
或需要绕开正式页面才能完成的功能。

失败处置：任一适用操作未通过，测试任务反馈给原唯一开发负责人，由其在原 formal task（正式任务分支）核实并修复，
修复后由原测试任务只复验新增改动、失败步骤和实际受影响的必要回归；
不得重启全部历史审查。修复通过前不得邀请用户验收、标记 `READY_FOR_USER_REAL_ACCEPTANCE`（已准备用户真实验收）或 `VERIFIED`（已经验证状态），
不得分层合并。computer-use 检查通过只说明“具备用户人工验收条件”，最终通过仍以用户明确确认的真实功能验收结论为准。


## 8. SOP（标准作业程序）-06：代码与数据边界

### 8.1 代码模块

| 代码模块 | 边界 |
| --- | --- |
| `common-platform`（公共平台代码模块） | 公共任务、文件、幂等、Outbox（事务消息发件箱）、审计、状态和期间原语 |
| `system-center`（系统中心代码模块） | 租户、公司、组织、成员、角色、权限、菜单和平台配置 |
| `auth-center`（认证中心代码模块） | 登录凭据及服务端不透明 Session（服务端会话） |
| `portal-center`（门户中心代码模块） | 门户壳、导航、检索、消息、收藏和传输中心编排 |
| `master-data`（主数据代码模块） | 员工、往来主体、商品、单位、仓库和资金基础档案 |
| `farming-center`（养殖中心代码模块） | 养殖存栏与生产事实 |
| `warehouse-center`（仓储中心代码模块） | 库存余额和不可变库存流水的唯一写入者 |
| `trade-center`（交易中心代码模块） | 采购销售订单与履约来源 |
| `settlement-center`（结算中心代码模块） | 结算、收付款、核销、资金流水和余额的唯一写入者 |
| `report-center`（报表中心代码模块） | 只读正式事实和版本化报表投影 |
| `app-bootstrap`（应用装配启动模块） | 模块装配与运行入口，不拥有业务事实 |

### 8.2 强制边界

- 跨模块只能调用公开应用服务；禁止引用他域 Mapper（数据映射器） 或直接读写他域私表。
- 一个正式事实只能有一个 writer（写入者）；consumer（能力消费方） 不得复制第二套余额、流水或权威快照。
- 不新增未受控代码模块。
- 登录使用服务端不透明 Session（服务端会话），不使用 JWT（结构化对象表示法网络令牌）。
- 运行时不使用 `accounting_book_id`（已禁用的运行时会计账簿标识）。
- 已发布 Flyway（数据库迁移工具） 不可修改，新变化只能追加更高版本。
- 测试夹具、SQL（结构化查询语言） 手工直写和私表查询不得替代生产 provider（能力提供方） 或用户真实验收。

## 9. SOP（标准作业程序）-07：直接相关测试

模块开发完成后只运行直接相关验证：

- 当前模块单元/服务测试；
- 当前模块前端 typecheck（类型检查）、组件或页面测试；
- 涉及获批视觉合同的模块，按第 7.1 节核对页面结构和六类最小视觉回归项；只验证文字存在或总宽度不足以证明忠实度；
- 受修改公开契约影响的 provider/consumer（提供方/消费方） 测试；
- 必要的真实 MySQL（关系型数据库）/Flyway（数据库迁移工具） 测试；
- 权限、租户/company（公司）、幂等、并发、审计和 fail-closed（失败时安全拒绝） 场景；
- 直接业务链正向和安全拒绝场景。

直接相关自动化测试用于尽早发现代码、契约和回归问题；第 7.1 节忠实度检查用于证明正式页面符合获批视觉合同；
第 7.2 节 computer-use 真实页面操作检查用于证明用户能够通过正式页面完成业务。三类验证目的不同，任何一类通过都不能替代另外两类。

禁止为了形式反复运行全仓 Maven（面向爪哇编程语言的构建工具）、全前端、全 Compose（容器编排） 或整阶段门禁。若直接相关测试首次
暴露累计兼容问题，应精确定位 owner（负责人） 和最小修复范围；不能借测试失败扩大模块职责。

静态 grep（文本检索工具）、截图、旧报告和代码存在不能替代必要运行验证。

### 9.1 第五阶段导出能力专项约束

本节来自 2026-09-03 已进入阶段分支的导出统一修复规范，仅适用于冻结契约启用导出的
BM-052（采购订单模块）～BM-057（自提退单模块）。BM-058（单据中心模块）继续不提供导入、导出和打印能力。

- 列表查询与导出请求使用独立 DTO（数据传输对象）；`pageNo/pageSize`（页码和每页条数）不得进入导出请求、
  任务参数或幂等摘要，不通过放宽未知字段校验消除契约错误。
- 未勾选时按当前筛选与排序导出；已勾选时只导出所选记录与当前筛选、租户、公司、数据权限的交集。
  当前页全选传递去重后的业务对象标识，不传分页状态；多条选择归入同一导出任务及结果文件。
- 选择范围必须贯穿前端、API（应用程序接口）与 OpenAPI（开放接口规范）、冻结参数、幂等摘要、
  Worker（异步任务执行器）严格白名单、恢复解析、SQL（结构化查询语言）和结果文件；同范围不同排序的标识列表
  规范化后应得到同一摘要，不同选择范围不得误复用同一任务结果。
- 直接测试覆盖未选、单选、多选、全选、越权、空结果、脱敏、数量上限和任务失败；必须核对最终文件，
  仅有按钮成功提示或任务受理响应不足以证明导出正确。
- 本节不新增业务能力或共享资源权限。各模块在阶段总控明确调度后，由原唯一负责人修复自身链路，
  完整限制和验证矩阵以 `docs/p0/stages/s5/export-contract-and-unified-remediation.md`（第五阶段导出契约与统一修复规范）为准。

### 9.2 第五阶段增量复验与既有结论保留

依据当前阶段计划登记的 2026-09-05 用户纠正，已完成的 Standards/Spec（规范符合性与需求符合性）审查结论保留。
没有新增缺陷、契约变化或合并冲突时，不重启整套双轴审查，只复验新增改动、未关闭缺陷和受影响的必要回归。
存在新变化时，按其实际影响确定复验边界，不拿旧结论替代新改动验证。

公共依赖任务只补最小公开能力，模块真实业务代码仍由原可见负责人承担；内部辅助任务不代替负责人，
不为重复 PREPARE（只读预读准备）或重复审查新建任务。本条是第五阶段执行纠正，不恢复通用双轴机器门禁。

### 9.3 最小修复、最小验证与禁止门禁偏移

本节来自同目录《最小修复、最小验证与禁止门禁偏移-人工裁定提示词》中用户已经确认的核心表述。
它约束问题定位、缺陷修复、测试失败处理及必要复验，不取消现行必要验证和用户验收，也不额外授予范围外修复权限。
截至本次事实绑定，该规则尚未同步进仓库根 `AGENTS.md`、`p0-control.yml`、Schema（结构约束）和轻量测试；
因此本 SOP 记录其已确认的人工边界，但不得宣称机器约束已经完成接入。

1. 默认只处理原任务明确的问题，只验证原失败问题、本次修复及本次修改实际直接影响的行为。
   不得仅因模块关联、上下游关系、同属一条业务链、共用组件或“保险起见”而递归追加关联模块测试、整链测试或全仓测试。
2. 正常根因定位可只读查看直接调用链，但不能把读取到的全部依赖自动转成待修复、待测试或当前任务完成前置；
   用户只要求诊断时，不据此修改代码。
3. 修复保持必要最小范围，不顺带重构、清理、升级依赖或补做范围外功能；最小按解决问题的必要性判断，
   不机械限定为一行代码或一个文件。
4. 确需扩大测试范围时，执行前在原任务中简短说明三项：本次改动直接影响什么；实际依据是什么；
   最小验证范围是什么以及到哪里结束。影响说明不需要另建文档、审查任务、审批链或证据提交。
5. 扩测不等于扩修。已有授权内、有实际依据的必要回归可以继续；超出原批准范围的修复、新增验收标准或新增门禁，
   必须由用户裁定。与本次改动无关的问题只记录和报告，不自行修复，也不自动成为当前任务的新完成条件。
6. 保留未受影响且仍适用于当前修改状态的有效结论，只复验新增改动、未关闭失败和实际受影响范围。
   不得删测试、放宽断言、屏蔽失败或降低安全要求取得通过。
7. 现行阶段已要求的构建、迁移、接口、安全、真实运行、用户验收和阶段末集中验证仍在其原定时点执行；
   不得借“必要验证”把阶段末或后续晋级门禁提前附加到每次局部修复。

### 9.4 独立测试交接、页面测试与原负责人修复

本节与第 4、7.2、10.5 节共同构成一次页面操作检查及其反馈闭环，不额外添加一轮同范围检查。
“完整测试流程”指本次已经批准、当前可独立验收的范围，不等于整个阶段、全部关联模块或所有衍生场景。

#### 9.4.1 开发侧交接条件

原开发负责人完成真实实现、直接相关必要自动化测试和适用的正式界面忠实度核对，
在获分配的公共窗口部署精确正式候选，并确认应用启动、正式登录、目标页面可达和验收数据前置可用，再交给测试任务。
不要求先重复完成独立测试的整套页面操作，也不将阶段末联动、全仓回归或后续晋级门禁提前到测试交接。

#### 9.4.2 完整测试流程交付内容

原开发任务直接向测试任务提供下列信息，允许采用清晰的任务消息，不强制新建说明文档、证据子提交或审批链：

1. 阶段、模块、原开发任务、关联测试任务、本次范围及精确正式候选提交/目录树。
2. 已批准需求、完整获批原型及必要的版本定位；说明适用项、延期项和不适用项。
3. 原型/正式环境用途区分、对应正式验收地址和目标页面、登录方式、所需角色/权限及数据前置。
   凭据通过既有安全入口读取，不在任务协调消息、测试记录或日志中传递明文账号密码。
4. 按顺序可执行的页面入口、输入和操作步骤、每步预期结果，以及适用的保存、刷新、查询、状态推进、文件结果核对方式。
5. 已批准范围内必要的失败路径、权限拒绝、异常反馈及操作限制。
6. 专用验收数据的使用范围、保留或获准重置方式、资源窗口及会话交接要求；不据此授权物理删除资源。
7. 已完成自测的结果摘要、已知问题、受阻或未覆盖项及原因，不能把未实现或未测内容写成通过。

#### 9.4.3 测试执行与权限边界

- 独立测试任务使用 computer-use（计算机操作技能）打开对应真实正式验收地址，按第 7.2 节逐项操作并反馈结果。
  接口、日志、数据库和代码只作为辅助核对，静态截图或接口成功不能代替真实页面测试。
- 测试同时对照已批准需求、完整获批原型及交接流程；若开发流程遗漏了已批准范围内的必要行为，可以说明依据后补充最小步骤。
  不得自行补充新需求、新验收标准，或把尚未批准、已明确延期的能力变成当前阻断门禁。
- 复用阶段已建立的公共正式验收环境及对应操作窗口，不因新建测试任务就再建一套完整服务栈；原型环境仍服务界面决策，
  不能代替真实功能测试，也不得被正式测试覆盖或抢占。
- 可以通过正式页面执行已授权的验收数据写入；不得修改业务源码、数据库迁移、部署配置或验收标准，不得直接改数据库绕过页面缺陷。
- 同一公共环境同一时刻只保留一个写入操作者，开发复现、测试操作、人工验收及候选切换顺序交接。
  保持候选版本稳定，不抢占另一任务的页面、会话或数据；具体页面权限见第 4.6 节。
- 环境、登录、权限、数据前置或计算机操作能力不可用时，报告受阻及最小原因，不能以替代性静态检查宣称通过。

#### 9.4.4 问题反馈与原开发负责人修复

测试任务把问题直接反馈给原开发任务，最少说明：候选版本、页面/入口、前置条件、最小复现步骤、
预期与实际结果、必要可见证据以及对本次验收的影响。完整操作记录留在原测试任务，不逐条抄送主控。

原开发负责人核实反馈、在自己的页面复现问题，并在原获授权的正式任务及代码边界内作必要最小修复；
无法复现或根因未明时如实说明，不直接否定问题或编造复现成功。测试任务不得接管修复或成为第二代码负责人。
修复后开发任务执行直接受影响的必要测试，按窗口更新精确候选，并向原测试任务说明修复内容、影响和复验步骤。
若问题属于其他模块、环境或共享资源，只向对应负责人提出精确协调请求，不自行跨域修复。

#### 9.4.5 必要复验、结果及人工汇报

- 原测试任务只复验原失败步骤、本次修改的行为和有实际直接影响依据的最小回归；保留未受影响且仍适用的有效结果。
- 扩大验证前说明改动直接影响什么、依据是什么、最小范围到哪里结束。关联性本身不是扩测依据，范围外修复或新增验收门禁由用户裁定。
- 不为每轮失败新建测试任务，不重复全部历史操作，不要求测试任务重复开发侧全部自动化测试，也不通过删测试或放宽标准取得通过。
- 结论区分“测试通过”“测试未通过”“受阻或未覆盖”，绑定候选及已批准范围；整体仍有适用步骤未通过或未覆盖时，不得报告全部通过。
- 测试任务向人工报告实际测试结论、问题及未覆盖项，同时把修复问题交回原开发任务；测试汇报不是代替用户作出验收裁定。
- 测试通过后，由原开发任务整理验收入口和必要说明并邀请用户真实功能验收；只有用户明确验收通过后，才进入原有分层合并流程。

### 9.5 本次合并的适用衔接与待人工裁定事项

本次授权是将两份规则合并更新到本文，不是同步仓库持久化配置或立即创建实际角色任务。
涉及仓库执行口径冲突时，仍须按第 1 节报告最小差异并在获得相应授权后同步；不得宣称本文编辑已完成机器规则接入。
后续接入须用独立测试接手原有页面检查，替换“禁止独立测试任务”和“开发自己完整执行后测试再重复执行”的旧口径，
保留唯一开发负责人、必要自测、用户验收及正常合并要求。

执行衔接按以下边界处理：后续生效时，适用于尚未进入用户真实功能验收的当前模块及后续模块；
已经进入人工验收的模块不因角色切换无故中断，已有效完成的测试不因换执行者重做，已经验收并合入阶段的模块不追溯补测。
阶段末集中验证仍按原时点和范围执行；本节的未决组织细节不得自行追加为全阶段停工、重复审查或新验收门禁。

以下原则在两份来源合并后已有明确口径，不再作为待决定项：必须有两个副主控；主控不自行开验收页面；
独立测试任务左侧可见；开发与测试各可开一个对应页面；问题回原开发修复；复验复用原测试任务；
测试向人工反馈，原开发任务负责最终真实验收邀请。

以下仍待人工裁定，本文不替用户决定：

1. 两个副主控按模块、开发/测试环节还是其他方式分工；是否左侧独立可见、何时建立/结束，以及能独立处理哪些协调事项。
2. 测试任务的总体数量、是否每模块配置独立测试负责人、是否可共享测试人员，以及多模块之间如何分配。
   无论采用哪种分配，已建立的模块测试上下文都要可追溯，问题复验不得随意换任务重开一轮。
3. 是否所有调度、协调和最终结果都必须经副主控汇总，以及允许直接向主控报告的例外条件。
4. “一个页面”的计数单位、跨页操作与弹窗/文件窗口的处理、额外开页授权，以及副主控的页面访问边界。
5. 独立测试任务测试结束后的顶置、关闭及保留方式；在明确前，不自动套用“用户已验收并合入阶段”的开发任务完成标签，
   也不擅自归档、删除或清理任务和材料。

## 10. SOP（标准作业程序）-08：真实功能人工验收

### 10.1 验收前置

- 本次范围涉及 UI（用户界面）或新增交互时，相关方案已明确批准；
- formal（正式开发） 实现和直接相关测试完成；
- 自 S5（第五阶段）起，涉及获批视觉合同的模块在向用户发起真实验收请求或邀请前已通过 `FORMAL_UI_FIDELITY_CHECK`（正式界面忠实度检查）；
- 自 S5（第五阶段）起，涉及正式页面或用户交互的模块已在精确部署候选上通过 `FORMAL_UI_OPERABILITY_CHECK`（正式界面真实可操作性检查）；
  按本次合并口径由独立测试任务执行，适用衔接见第 9.5 节，不重复增加一轮相同检查；
- 依赖在 `REAL_ACCEPTANCE`（真实功能验收时点） 时点满足，或按批准方案 fail-closed（失败时安全拒绝）；
- 使用可追溯版本的真实前后端、数据库和公开 UI（用户界面）/API（应用程序接口）；
- 自 S5（第五阶段）起，取得阶段总控分配的公共真实验收窗口，并完成下述数据重置与运行就绪检查；
- 验收环境身份达到 `IDENTITY_READY`（验收身份准备就绪）；
- 自 S6（第六阶段）起，当前正式候选使用已批准原型对应的统一门户上下文，门户身份、能力、完整导航和全局入口无未处理偏离；
- 报告“等待真实验收”前已执行 `USER_REVIEW_ENTRY_LOGIN_READY`（用户验收入口登录就绪）。

### 10.2 一套公共环境、真实验收串行

自 S5（第五阶段）起，每阶段默认只运行一套公共真实验收环境，由阶段总控管理。所有模块使用同一固定访问地址，
容器编排项目名、主机名、监听端口、前后端、MySQL（关系型数据库）、Redis（缓存服务）、MinIO（对象存储服务）、
登录入口与基础身份均应固定且可预测。公共原型网关另行提供界面预览，不进入此队列。

执行 `SINGLE_SHARED_STAGE_ACCEPTANCE_ENVIRONMENT`（阶段单一公共验收环境）与
`REAL_ACCEPTANCE_SERIAL`（真实验收串行）。只有正式实现和直接测试完成、准备请求用户验收的模块才进入队列；
同一时刻仅一个模块、一个操作者开展写入型真实验收；开发复现、测试操作和人工操作不能相互抢占。
开发并行或独立测试任务可见不授予环境占用权，六个开发槽位不代表六套验收环境。
测试任务复用阶段预先建立的正式验收环境，不因角色拆分另建长期完整服务栈；原型与正式环境职责保持区分。

资源窗口用于精确部署、数据重置和验收准备；取得窗口不等于可以立即邀请用户。正式页面运行后，须完成包括忠实度和
computer-use 真实页面操作在内的适用就绪检查，才向用户发起验收请求。

S1（第一阶段）～S4（第四阶段）已经完成的历史环境事实按当时规则保留，不按新规则重建或改写。

### 10.3 精确部署与切换数据

进入窗口时部署本模块精确的 formal task commit/tree（正式任务提交及目录树），并同时记录阶段固定基点和
Flyway（数据库迁移工具）最高版本；不能只登记无法定位的“最新代码”。

公共容器和地址可以复用，数据不能无条件沿用。切换模块前，按实际情况采用一种最小安全重置：

1. 重建干净数据库；
2. 重建独立 schema（数据库模式）；
3. 恢复阶段基线数据快照；
4. 清理并重新初始化明确的数据命名空间。

重置后迁移版本必须满足本次正式任务目录树。可以在公共数据库服务内建立临时数据库或模式，
但不能默认为每个模块长期保留完整独立服务栈。

### 10.4 基础身份与最小运行就绪检查

验收身份可通过幂等 bootstrap（引导初始化） 准备 tenant（租户）/company（公司）/org（组织）、专用 user（用户）/employee（员工）、最小 role（角色） 和
data scope（数据范围），但不得预造主数据、业务配置、单据、余额、流水或其他业务 provider（能力提供方）；业务事实
必须通过正式 UI（用户界面）/API（应用程序接口） 产生。

凭据不得进入 Git（分布式版本控制系统）、Flyway（数据库迁移工具）、测试/命令日志、证据回执、Agent 间协调消息或普通进度消息。
只有自动登录确实不可用时，专用非生产验收账号和临时密码才可出现在模块直接面向用户的最终验收汇报中，并在验收窗口结束后轮换或停用。
登录链每套环境只做一次 smoke（冒烟检查）；多身份切换应先建立下一身份有效 Session（服务端会话），再退出上一身份。
验收账号默认不强制改密；批量前置配置可使用正式接口辅助工具，不得直写业务表、伪造能力提供方或替代用户真实功能验收。

邀请用户前，逐项确认后端健康、前端可打开、正式登录成功、公共门户和工作区可进入、目标菜单存在、正式路由可打开、
适用的正式界面忠实度检查与真实可操作性检查均通过、运行代码与声明的正式任务提交及目录树一致，以及数据库迁移版本正确。
当前机器就绪清单已包含 `FORMAL_UI_FIDELITY_CHECK_PASSED`（正式界面忠实度检查已通过）和
`USER_REVIEW_ENTRY_LOGIN_READY`（用户验收入口登录就绪）。此处是最小运行就绪检查，
不新增重型门禁、证据子提交或重复全仓测试。

### 10.5 独立页面操作检查、原任务修复与验收邀请

1. 原开发任务完成直接相关自测、适用忠实度核对，在公共窗口部署精确正式候选，并完成第 10.4 节中的启动、登录和目标路由等基础就绪准备。
2. 原开发任务按第 9.4.2 节直接向独立可见测试任务交付完整流程；不把流程先全部发送主控再由主控转发。
3. 独立测试任务使用 computer-use（计算机操作技能）在对应正式页面执行第 7.2 节检查，并对照需求及获批原型核对当前批准范围。
4. 出现问题时，测试任务直接反馈给原开发任务并向人工报告实际结论；原开发核实、复现、修复并重新交付精确候选，原测试任务最小复验。
5. 检查通过后，原开发任务核对第 10.4 节完整邀请条件及第 10.6 节入口登录就绪要求，直接邀请用户真实功能验收。

主控只协调版本信息、环境窗口和精确跨模块资源问题，不自行打开验收页面，也不接收普通修复流水。
开发和测试各自开页及窗口交接遵守第 4.6 节；独立测试接手既有检查，不要求开发任务先重复执行一套完整页面流程。
具体执行衔接遵守第 9.5 节。

操作顺序必须从正式登录和菜单进入开始，不得从预先打开的隐藏路由或开发工具状态开始。每个关键动作依次记录“页面输入或点击、可见反馈、
后端或下游结果、刷新后状态”。涉及下载或打印时记录本次新结果；涉及跨模块链路时记录上游与下游业务对象编号及最终回写。

操作检查通过后，原开发负责人才能向用户发起真实验收邀请，并提供：正式页面地址、精确候选 commit/tree（提交/目录树）、可验收功能、验收数据编号、建议操作步骤、已知不适用项和
仍保留的明确延期能力。禁止只报告接口、测试数量、Agent active（智能体处于运行状态）、标题更新、按钮存在或页面 HTTP 200（超文本传输协议成功响应）。

用户随后进行的人工验收仍是独立最终裁定。用户发现视觉、控件、功能、文件、数据、权限或业务链问题时，computer-use 记录不能推翻用户结论；
模块必须按新增缺陷回到原负责人修复和必要复验。

### 10.6 验收入口自动登录与凭据兜底

模块把标题切换为或向用户报告“等待 UI 验收”“等待真实验收”前，必须执行
`USER_REVIEW_ENTRY_LOGIN_READY`；机器策略为
`AUTO_LOGIN_OR_DIRECT_TEMPORARY_CREDENTIAL_HANDOFF`（自动登录或直接交付临时凭据）：

1. 优先读取仓库外 acceptance secret/env（验收秘密配置/环境变量），通过浏览器自动完成正式登录，并把当前可见页面停留在本模块
   精确目标路由；只打开登录页不构成可验收交付。
2. 自动登录确实不可用时，模块直接面向用户的最终验收汇报必须同时提供专用非生产验收用户名、临时密码和登录地址。
   明文凭据不得出现在 Git、迁移、测试/命令日志、证据回执、Agent 间协调或普通进度中，验收窗口结束后应轮换或停用。
3. 本规则同时适用于 UI 原型批准和真实功能验收。阶段总控只协调身份、入口和公共环境，不自行开页或代替模块请求用户批准；
   开页、登录和目标页面准备由有对应权限的开发/测试任务在各自职责与窗口内执行。

### 10.7 验收结论与窗口交接

模块任务直接请求用户验收真实功能。用户通过前不得分层合并或声明 `VERIFIED`（已经验证状态）。用户通过
后，模块任务向阶段总控报告：

- 获批 UI（用户界面） commit/tree（提交/目录树）；
- 适用时，正式界面映射与忠实度核对结果；
- 适用时，独立页面测试最终结论、覆盖/未覆盖摘要、精确候选版本及原测试任务记录定位，不主动附逐项操作和修复流水；
- formal（正式开发） commit/tree（提交/目录树） 和 clean（干净无未提交变更） 状态；
- 直接相关测试；
- 真实验收场景与结论；
- 依赖关闭/保留状态；
- 当前公共验收窗口占用及待释放状态、其他共享资源释放情况；
- 可合并分支链。

完整测试步骤、问题证据、修复讨论和复验记录保留在原测试/开发任务。主控接收上述最终结果与必要协调摘要，
确需核实冲突时仅请求相关最小事实，不常规回收全部上下文；副主控也不得把完整日志原样转发主控。

随后由阶段总控执行以下固定顺序：

```
用户真实功能验收通过
→ task → feat → group → stage    （任务分支依次合入功能分支、分组分支和阶段分支）
→ 释放公共验收窗口
→ 下一模块取得窗口
```

用户通过但尚未完成分层合并时，不提前释放公共验收窗口。模块结束后停止其专属开发资源，
公共环境继续供后续模块使用；阶段收口时统一停止并保留。

若用户判定正式页面复刻失败，按第 7.1 节回到原任务修复；不能把原型曾获批或环境已就绪当成正式业务验收通过。

### 10.8 独立环境的临时例外

只有用户明确批准并发真实验收，或存在候选迁移无法安全重置、外部依赖或版本互斥、会话身份或数据无法可靠隔离、
公共环境无法解决的已确认技术阻断时，由阶段总控向用户报告后才可临时创建独立环境。

获批并发时，例外环境须隔离容器编排项目名、主机名、监听端口、数据库、数据卷、数据命名空间和必要浏览器会话。
仅更换 `127.0.0.1`（本机回环网络地址）的端口不能隔离 Cookie（浏览器会话凭据）。

模块直接相关自动化测试和必要 Testcontainers（临时容器集成测试工具）测试仍使用各自所需隔离，
不因公共真实验收串行而被一律串行化；实际争用同一重型资源时由总控安排窗口。

## 11. SOP（标准作业程序）-09：分层合并

### 11.1 分支模板

```
prototype/p0/s{stage}/{scope}    （第一期阶段交互原型分支模板）
task/s{stage}/{group}/{module}/{role}    （任务分支模板）
feat/s{stage}/{group}/{module}    （功能分支模板）
group/s{stage}/{group}    （分组分支模板）
stage/s{stage}    （阶段分支模板）
integration/p0    （第一期集成分支）
release/p0    （第一期发布分支）
main    （主分支）
```

辅助分支：

```
fix/p0/{module}/{issue}    （一期模块问题修复分支模板）
int/{stage}/{scope}    （阶段跨组联调分支模板）
```

Worktree（工作树） 模板：

```
D:\工作\项目-worktrees\{stage}-{group}-{module}-{role}    （模块工作树路径模板）
```

### 11.2 合并规则

唯一业务合并流向：

```
task → feat → group → stage    （任务分支依次合入功能分支、分组分支和阶段分支）
```

要求：

- 业务功能任务在用户真实功能验收通过后才进入分层合并；第 12.3 节仅测试夹具专项按其明确授权与直接验证结果累计。
- prototype（交互原型） ancestry（祖先链） 永不进入 formal（正式开发） ancestry（祖先链）。
- formal（正式开发） task（任务分支） 只正向累计，不把 Stage（阶段） 反向注入旧 task（任务分支）。
- Group（分组）、Stage（阶段） 等共享分支禁止 rebase/force-push（变基/强制推送）。
- 禁止自审 Stage（阶段）、直接向 `main`（主分支） 开发或 push（推送）。
- `stage → integration/p0 → release/p0 → main`（阶段分支依次进入第一期集成分支、第一期发布分支和主分支） 每一步都要用户明确批准。
- 自 S5（第五阶段）起，模块真实验收通过且分层合入阶段后，才释放公共验收窗口；不因合并单个模块停止共享环境。

### 11.3 技术续作

跨阶段 technical-task（技术续作任务） 只沿其 exact（精确登记） registration（注册记录） 指定的 task（任务分支）→feat（功能分支）→group（分组）→stage（阶段） 正向链合并。
registration（注册记录） 只是执行身份，不自动关闭依赖、不授权共享资源、不替代 UI（用户界面）/真实功能验收，也
不得新增 BM/TI（业务模块/技术项） 或业务功能。

用户已明确批准的零生产改动、单测试文件夹具专项，可以按第 12.3 节免除不相关的界面原型及重复真实业务验收；
这种例外不扩展到生产实现、接口、迁移或新交互修改。

## 12. SOP（标准作业程序）-10：阶段完成统一验证

### 12.1 累计阶段统一验证

模块开发期不重复跑全仓；阶段结束时，由阶段总控在唯一累计 clean（干净无未提交变更） Stage（阶段） tree（版本控制目录树） 上集中执行
一轮联动和整体验证。自 S5（第五阶段）起，所有模块累计后只重建一次公共真实验收环境，
从干净数据库执行 Flyway（数据库迁移工具）全部正式迁移，并构建、启动同一累计阶段的前后端：

自 S6（第六阶段）起，跨模块联动、完整业务链、E2E（端到端测试）、累计验收和全局 UI 清理在模块开发期间均应明确保持
`DEFERRED`；机器状态语义为 `DEFERRED_NOT_PASS_VERIFIED_OR_COMPLETE`（延期项不得标记为通过、已验证或完成）。
阶段总控只在全部模块进入唯一最新 clean Stage tree 后集中执行。
此延期不解除 business-link plan（业务联动规划）已经声明的 `HARD_RUNTIME`（运行时硬依赖）或
`HARD_ACCEPTANCE`（验收时硬依赖），硬依赖仍须在各自阻断时点前关闭。

1. 快照和控制文件轻量检查。
2. 根 Maven（面向爪哇编程语言的构建工具）完整测试。
3. 前端全量测试、typecheck（类型检查）和 production build（生产构建）。
4. Compose（容器编排） 及真实运行配置。
5. 全新 MySQL（关系型数据库）空库上的 Flyway（数据库迁移工具）V1（第一版迁移）至当前累计最高版本迁移、历史记录及校验。
6. 契约、权限、租户/company（公司）、审计、幂等和并发验证。
7. business-link plan（业务联动规划） 登记的累计联动场景。
8. 上一阶段 provider（能力提供方） 的真实消费和 fail-closed（失败时安全拒绝） 场景。
9. 唯一事实 writer（写入者）、引用保护和共享资源顺序。
10. 下一阶段 provider（能力提供方） 目录。

在同一固定地址完成阶段全量功能与跨模块业务链验收，不逐个恢复历史模块独立环境。
“集中一轮”不表示失败后禁止复验，也不自动要求每次失败都把所有无关模块从头重跑。
失败时定位精确原因，由责任模块按已批准边界最小修复并正向累计，再按下述适用裁定复验；
不恢复旧双轴审查、重复 review（审查）或第二套正式门禁。

### 12.2 验证并行与共享资源顺序

同一累计目录树上，互不写共享事实的后端测试、前端测试与构建、轻量治理检查、分支祖先关系及能力目录核对，
可按实际资源条件并行。全新数据库迁移、容器编排启动和停止、共用数据库或会话的业务链、库存与资金唯一写入者
相关共享资源验证必须串行安排。测试内部的临时隔离数据库不得接入或修改公共真实验收业务数据。

### 12.3 测试夹具批量诊断与原负责人修复

2026-09-01 人工裁定针对 S4（第四阶段）最终收尾引入诊断性批量盘点；S5（第五阶段）～S7（第七阶段）
出现同类问题时可按裁定范围沿用，并须在执行前把适用授权同步到仓库控制登记与阶段计划。
它不是每个阶段都必须额外运行的一轮测试。

- 只适用于已证明为旧测试数据、固定日期、陈旧数量断言、换行符、测试事务边界或累计能力接入导致的单测试文件夹具漂移。
  生产实现、正式迁移、接口及运行环境须无对应故障；不得削弱权限、隔离、幂等、原子性或失败拒绝断言。
- 已授权的当前修复完成并累计后，可从最新干净阶段基线运行一次诊断性 `mvn -fae test`（构建工具在可继续模块中收集测试失败）。
  此命令仅生成失败清单，不是阶段通过证据，也不放宽前端、治理、容器编排或真实业务链的失败策略。
- 总控批量登记技术续作，明确原可见任务、唯一允许测试文件、执行阶段、分组、角色和正式分支；仍由原唯一负责人修复。
  不同模块的只读定位和单文件修改可并行，重型共享测试资源由总控或运行副手串行调度。
- 每项修复须保留原失败可复现的 RED（失败复现）证据、原失败方法及完整测试类或最小受影响回归的 GREEN（通过）结果，
  并通过 `git diff --check`（版本控制差异格式检查），确认精确改动仅为获批测试文件且工作树干净，再分层累计。
- 此类零生产改动修复不需要界面原型、界面批准或重复真实业务验收。涉及生产代码、编译、数据库迁移、正式接口、
  界面、安全、数据完整性或无法证明仅为夹具的问题时，立即停止该批量方式并报告精确阻断，另按具体授权处置。
- 诊断临时 Testcontainers（临时容器集成测试工具）资源使用独立数据库、随机端口、独立容器及匿名卷，
  生命周期结束由测试框架停止和移除；不把它们混同为需长期保留的正式验收材料。

### 12.4 最终验证恢复边界与历史裁定

9 月 1 日批量盘点裁定原要求“诊断后完成全部已批准修复，再从最新干净阶段完整认证一次”，最终认证恢复首次精确失败即停；
最终认证如又遇新的夹具问题，须再次取得明确授权，不能无限循环自行修复。

随后 S4（第四阶段）阶段计划记录了更具体的用户授权：BM-007（电脑打印设置模块）从后端失败点恢复，
BM-002（公司设置模块）及 BM-029（收蛋记录模块）从前端失败点恢复，不重跑当时已经完成且未受影响的验证。
这些是相应任务、版本和时间点的执行裁定，不是任何阶段都可以跳过完整认证的通用许可。

因此执行时须同时核对原批量裁定、当前阶段最新计划及后续明确授权，采用实际适用的恢复边界；未受影响旧结论可以按授权保留，
新增改动与未关闭失败必须验证，诊断失败清单、过时目录树或不相关旧报告都不能替代当前结果。
第五阶段增量复验另按第 9.2 节执行，不复活整套重复审查。

### 12.5 测试基础设施提速：已记录，尚未证明落地

2026-09-01 已记录 MySQL（关系型数据库）Testcontainers（临时容器集成测试工具）与
Spring Context Cache（应用上下文缓存）复用提速裁定。当前核对到人工裁定输入，未在本次绑定的控制登记和变更记录中
找到对应提速专项落地证据；不把它写成已完成优化或当前开发前置。

后续实施须先登记测试基础设施技术任务和唯一负责人，从最耗时测试套件的小范围试点开始。建议方向为同一测试进程内
复用容器和应用上下文、减少重复迁移，并证明用例之间数据隔离；不得跨测试运行借用旧容器状态冒充全新数据库。
必须保留真实数据库语义、阶段全新空库完整迁移、原测试集合和关键断言，并提供优化前后容器启动、迁移次数、
上下文创建与总耗时对照。不得借提速修改生产业务或中断正在运行的最终阶段验证。

公共真实验收环境复用已是现行规则，测试基础设施复用是否完成是另一项事实，两者不能相互替代。

### 12.6 现行轻量治理命令

以下为开发执行入口示例；本次文档同步实际运行的只读检查及结果记录在第 19 节，不把文档核对当成业务测试完成：

```
python scripts/p0_contracts.py verify-snapshot docs/p0/contracts/P0.4-R4    # （使用蟒蛇编程语言验证第一期第四版修订四契约快照）
python scripts/p0_governance.py validate-control p0-control.yml --snapshot docs/p0/contracts/P0.4-R4    # （使用蟒蛇编程语言按快照验证项目控制文件）
python scripts/p0_governance.py check-stage-business-link-plan <plan> --control p0-control.yml    # （使用蟒蛇编程语言检查阶段业务联动规划）
python -m unittest scripts.tests.test_p0_contracts scripts.tests.test_current_development_flow    # （使用蟒蛇编程语言运行契约与现行开发流程单元测试）
```

具体 Maven（面向爪哇编程语言的构建工具）、前端、Compose（容器编排）、Flyway/MySQL（数据库迁移工具/数据库） 和业务链命令由当前累计 Stage（阶段） 的实际工程配置
确定，不引用退役脚本。

## 13. SOP（标准作业程序）-11：G5（第五门禁）、G6（第六门禁） 与发布

当前活动治理门禁只有 G5（第五门禁） 和 G6（第六门禁）：

- G5（第五门禁）：用户明确批准 `stage → integration/p0`（阶段分支进入第一期集成分支） 后执行，用于 Integration（集成阶段） promotion（晋级） 和跨阶段/
  业务链验证；不是模块开发前置。
- G6（第六门禁）：用户明确批准 `integration/p0 → release/p0`（第一期集成分支进入第一期发布分支） 后执行，用于 Release（发布阶段） promotion（晋级）、完整发布
  条件和发布候选验证；不是 Stage（阶段） 模块开发前置。

最终流向：

```
stage/sN    （阶段编号占位符对应的阶段分支模板）
→ 用户批准
→ integration/p0 + G5    （进入第一期集成分支并执行第五门禁）
→ 用户批准
→ release/p0 + G6    （进入第一期发布分支并执行第六门禁）
→ 用户批准
→ main    （进入主分支）
```

采购、销售、养殖三条端到端链分别为：

```
CHAIN-PUR：S2 → S4 → S5 → S6 → S7    （采购业务链：第二阶段到第四、第五、第六和第七阶段）
CHAIN-SAL：S2 → S4 → S5 → S6 → S7    （销售业务链：第二阶段到第四、第五、第六和第七阶段）
CHAIN-FARM：S2 → S3 → S4 → S7    （养殖业务链：第二阶段到第三、第四和第七阶段）
```

## 14. SOP（标准作业程序）-12：任务状态、顶置与资源保留

模块开发任务标题格式：

```
S{stage} BM-{id} {模块名}（{当前周期}）    （阶段编号、业务模块编号、模块名和当前周期组成的任务标题模板）
```

周期至少使用：

- `Prototype PREPARE`（原型只读预读准备）
- `UI 原型开发中`（用户界面原型开发中）
- `等待 UI 验收`（等待用户界面验收）
- `等待统一门户整页复批`（批准后发现门户上下文偏离，完成纠正并等待用户重新批准完整页面）
- `正式 PREPARE`（正式开发只读预读准备）
- `正式开发中`（正式业务开发中）
- `轻量测试中`（直接相关轻量测试中）
- `等待独立页面测试`（已交付精确候选和完整流程，由关联的可见测试任务进行页面检查）
- `按测试反馈修复中`（原开发负责人核实并修复问题，完成后交回原测试任务必要复验）
- `等待真实验收`（等待用户真实功能验收）
- `验收通过待合并`（真实功能验收通过并等待分层合并）
- `已验收并合入 Stage`（已经验收并合入阶段分支）
- `精确 BLOCKED：{最小原因}`（精确阻塞：最小原因）

独立测试任务使用与开发任务可区分的名称和状态，名称包含阶段、模块及“测试”，并关联原开发任务。
状态可标记“待测试”“测试中”“等待原开发修复”“必要复验中”“测试通过”“测试未通过”“受阻或未覆盖”，
并说明对应候选及范围。测试通过不代表用户已验收、模块已完成或已合入阶段。
问题、修复反馈和复验继续留在原测试任务，不每轮另建；测试任务的顶置、关闭和保留方式按第 9.5 节待裁定边界执行。

未彻底完成的模块开发任务保持普通可见、不顶置。只有同时满足以下条件才标记
“（已验收并合入 Stage（阶段））”并顶置：

1. 用户真实功能验收通过；
2. 已按 task（任务分支）→feat（功能分支）→group（分组）→stage（阶段） 合入累计 Stage（阶段）；
3. 模块专属开发资源已经停止；自 S5（第五阶段）起，模块公共真实验收窗口已经释放，阶段共享环境继续运行；
4. 最终 commit（提交）、tree（版本控制目录树） 和工作树 clean（干净无未提交变更） 已核对。

若用户判定正式页面复刻失败，或独立页面检查发现控件、文件、状态、副作用或跨页业务链不可用，原开发任务回到正式开发修复态，
修复后交回原测试任务作必要复验；
未修复并通过第 7.1、7.2 节适用检查前，不得进入上述完成或顶置状态。
若批准后发现统一门户上下文不一致，按第 6.7 节保留业务内容工作并局部冻结，任务进入“等待统一门户整页复批”；
用户明确复批完整页面前，不得恢复门户相关正式 UI 定稿、忠实度结论、真实验收、分层合并或 `VERIFIED`。

S1（第一阶段）～S4（第四阶段）的历史验收资源按当时规则处理。阶段共享验收环境在阶段收口时统一停止并保留，
不因单个模块结束而停止。相关分支、工作树、提交和验收材料继续保留；归档、删除或物理清理须取得用户批准。
第 12.3 节测试框架自主管理的临时诊断资源按其明确生命周期处理。

## 15. 阶段依赖与范围

现行 Stage（阶段） DAG（有向无环依赖图）：

```
S1 → S2    （第一阶段进入第二阶段）
S2 → S3    （第二阶段进入第三阶段）
S2 → S4    （第二阶段进入第四阶段）
S3 → S4    （第三阶段进入第四阶段）
S4 → S5 → S6    （第四阶段依次进入第五和第六阶段）
S3 + S4 + S6 → S7    （第三、第四和第六阶段共同支撑第七阶段）
```

| Stage（阶段） | Group（分组） | 主要范围 |
| --- | --- | --- |
| S1（第一阶段） | `S1-AUTH`（第一阶段认证分组）、`S1-COMMON`（第一阶段公共能力分组） | 登录、Session（服务端会话）、门户和公共工作区能力 |
| S2（第二阶段） | 6 个设置/主数据 Group（分组） | 设置、员工、往来主体、商品、仓库和资金主数据 |
| S3（第三阶段） | 5 个养殖 Group（分组） | 鸡群、生产、健康、配置和工作台 |
| S4（第四阶段） | 4 个仓配库存 Group（分组） | 仓配底座、出入库、库存查询、盘点和损益 |
| S5（第五阶段） | 4 个交易 Group（分组） | 采购、车销、自提和单据中心 |
| S6（第六阶段） | 3 个结算 Group（分组） | 客户、供应商和内部结算 |
| S7（第七阶段） | 4 个报表 Group（分组） | 养殖、业务、库存和资金报表 |

阶段依赖只表示 provider（能力提供方） 可用顺序；具体模块是否阻断必须看 business-link plan（业务联动规划） 中的 typed（类型化）
dependency（依赖关系），不得用 Stage（阶段） DAG（有向无环依赖图） 无差别停止整个阶段。

### 15.1 截至本次基线的第五阶段执行补充

以下来自第五阶段计划在 2026-09-07～2026-09-08 的增量记录，仅描述该阶段具体任务的范围和授权，
不新增全项目规则，也不表示本次文档同步重新运行了这些模块的验收或测试：

- 数据库迁移排号随实际候选调整：当前批次登记为 BM-053（采购退货模块）使用 V91（第九十一版迁移），
  BM-056（自提订单模块）使用 V92/V93（第九十二版和第九十三版迁移），BM-055（车销退单模块）使用 V94（第九十四版迁移）。
  BM-057（自提退单模块）最新打印专项另明确 V95/V96（第九十五版和第九十六版迁移）均未发布，
  只在独占租约内最小补齐权限和默认模板种子，不新增版本号。后续排号读取届时最新计划，不把本段固化成其他阶段模板。
- BM-055（车销退单模块）的权威库存批次及启用库位引用纳入直接依赖和服务端复校；计划记录其
  SOURCE_FREE（不关联原单）范围已获用户验收并分层合入、公共窗口已释放。SOURCE_LINKED（关联原单）仍受
  `DEP-S5-068`（第五阶段第六十八项依赖）局部阻断，不把局部验收通过扩大为整个模块全部能力完成。
- BM-057（自提退单模块）打印复用已有公共模板和打印提交、执行、渲染能力，由同一可见负责人持有最小共享租约，
  补对象注册、真实业务投影与金额权限变量及自身适配，不新建公共能力任务或重复审查任务；验证打印增量、受影响回归，
  并从全新数据库复验 `V1→V96`（第一版至第九十六版迁移）的完整链路。
  `DEP-S5-043/046`（第五阶段第四十三项和第四十六项依赖）在部署并取得用户真实验收前保持未关闭；
  该打印实施租约不自动授予公共真实验收窗口、分层合并或发布权限。

以上记录示例继续遵循“一个事实一个写入者、缺失依赖只阻断具体能力、复用已有公共能力、原负责人最小修复”的既有原则。

### 15.2 截至本次基线的第六阶段执行补充

以下来自当前 `stage/s6` 的业务联动计划、UI/能力复用基线和统一门户标准投影，既包含通用规则在 S6 的实例，
也包含本阶段当前执行事实；后续状态或目录变化时以届时最新 clean `stage/s6` 为准：

- S6 范围严格限于 `BM-059`～`BM-071` 及对应 `TI-060`～`TI-072`，采用“模块独立功能优先、阶段末完整联动”。
  `BM-059 收款单` 是首个 UI 母版；其完整原型已经由用户批准，`docs/p0/stages/s6/ui-reuse-baseline.md`
  已处于 `APPROVED_ACTIVE`，后续模块按该基线制作完整原型并逐模块取得用户视觉批准。
- formal（正式开发）依赖波次为：W1（BM-061、BM-063、BM-065、BM-066、BM-067、BM-068），
  W2（BM-059、BM-060、BM-062、BM-064），W3（BM-069），W4（BM-070、BM-071）。
  UI 母版顺序与 formal 依赖波次分开；硬依赖和共享写入资源仍按当前业务联动计划阻断。
- `settlement-center`（结算中心）是应收、应付、账户流水和账户余额的唯一 writer（写入者）。共享账本、余额和分摊基元只构建一次；
  `master-data`、`common-platform` 和 `trade-center` 的公开能力按计划复用，不得跨域直表或为每个结算模块复制一套权威事实。
- 当前 S6 统一门户上下文使用 tenant `11001 / acceptance-a / S1-S3 全链验收租户 A`、
  company `12001 / ACC-A / 华东蛋鸡有限公司（全链验收）`、user `15001 / 全链验收管理员`；标准投影为
  `7 modules / 25 groups / 84 items`，SHA-256 为
  `0594689cb4704ea216b07a5b1eaa96eee5fabefda870cbb87eaa8e0b7cf0881c`。
  精确身份编码、门户能力、全局入口、导航字段和规范化方法以 S6 复用基线及
  `docs/p0/stages/s6/portal-navigation-canonical.json` 为准，不在模块内另建版本。
- BM-063（预付款单）完整已登录页面与业务内容已由用户明确批准，登记
  `MODULE_UI_APPROVAL_INDEPENDENT=APPROVED` 与 `PROTOTYPE_PORTAL_CONTEXT_PARITY=PASS`。
  `DEP-S6-013 master-data → BM-063` 已 `RESOLVED`，允许从批准登记后的最新 clean `stage/s6` 创建独立 formal；
  prototype ancestry 禁止 merge/cherry-pick。BM-067 已释放 V100；V101 当前位于 Flyway 队首，BM-063 formal
  到达迁移写入点并确认精确文件/用途后由阶段总控明确授予；TI-064 的
  导入、导出、打印继续全部 `DENY/NOT_APPLICABLE`。
- BM-061（预收款单）完整已登录页面与业务内容已由用户明确批准，登记
  `MODULE_UI_APPROVAL_INDEPENDENT=APPROVED` 与 `PROTOTYPE_PORTAL_CONTEXT_PARITY=PASS`。
  `DEP-S6-012 master-data → BM-061` 已 `RESOLVED`，允许从批准登记后的最新 clean `stage/s6` 创建独立 formal；
  prototype ancestry 禁止 merge/cherry-pick。V102 顺排在 V101 之后且只在该前序租约释放后明确授予；
  TI-062 的导入、导出、打印继续全部 `DENY/NOT_APPLICABLE`，`50/100/200` 仅是已批准业务参数适配。
- 用户已批准 `S6-BM061-API-01 / APPROVED`，补齐 BM-061 实现所需而 TI-062 冻结表未列出的接口合同：
  `GET /api/v1/fund/customer/customer-advances` 查询列表、`POST /api/v1/fund/customer/customer-advances` 新建、
  `POST /api/v1/fund/customer/customer-advances/append-remark` 追加备注、
  `POST /api/v1/fund/customer/customer-advances/{id}/copy` 复制和
  `POST /api/v1/fund/customer/customer-advances/batch-confirm` 批量确认。新建、复制、批量确认必须幂等；
  复用既有权限，不新增权限码。复制只带出当前用户可读且目标新建允许编辑的业务头、账户行、折扣和备注，
  不复制身份、状态、附件、日志、使用记录、下游结果或生效事实，并生成新的草稿身份与审计字段；批量确认一次处理
  1～200 项，逐项执行范围、状态、版本和职责分离复校，允许合格项部分成功且单项原子。
  原 P0.4-R4 不可变快照不改写；该裁决作为 S6 阶段级增量合同解除 BM-061 对上述 API 的本地冻结，
  不授予 Flyway、共享资源、真实验收、分层合并或发布权限；TI-062 的导入、导出、打印 `DENY/NOT_APPLICABLE` 不变。
- 对全部并行 formal 候选补做只读盘点后，确认 BM-059/C clean candidate `5e7d17fb` 已在 V99 创建
  `sett_customer_advance_balance`、`sett_account_balance`、`sett_account_ledger`、
  `sett_advance_receipt_ledger`，并实现共享 `SettlementLedgerService`、`SettlementReceivableDirectory`；
  因此 BM-059/V99 是这些共享事实的首个 `BUILD_ONCE` 候选 owner。此前把 owner 临时指给 BM-063/V101
  的结论已前向撤销，BM-061 新建 `CustomerAdvanceBalanceLedger` 的租约也已撤销且未产生提交。
  V99 尚未进入当前 Stage，BM-061/BM-063 的旧 fixed point 可继续模块私有工作，但不得复制共享 DDL、
  余额算法或把未晋级候选 merge/rebase/cherry-pick 进任务绕过正常 promotion。
- V101 继续为 BM-063 队首预留但 `RESERVED_NOT_GRANTED`；V99 正常晋级且币种合同关闭后，只创建
  TI-064 私有表和 V99 未提供的 `sett_prepayment_ledger`。V102 继续为 BM-061 预留且必须排在 V101 后，
  只创建 TI-062 私有表并复用 V99 共享能力。BM-061 当前唯一共享源码 `GRANT` 是
  `TradeReferenceDirectory.filterEnabledCustomerIds(...)` 及直接测试，用于后端范围/片区过滤，不新增 UI 控件。
- TI-062/TI-064 均冻结了 `currency_id → mdm_currency.id`，但累计树不存在 `mdm_currency` 或公开 currency-id
  provider，只有 `mdm_account.currency_code`。用户已于 2026-09-10 批准
  `S6-CURRENCY-01 / APPROVED`：BM-061/BM-063 的新建和修改接口不接收 `currencyId`；后端必须从所选
  启用资金账户的公开应用服务取得并在写入动作复校 `currencyCode`；多账户币种必须一致，否则安全拒绝且
  不产生部分副作用；单据持久化 `currency_code CHAR(3)` 业务快照并在详情返回 `currencyCode`；页面不新增
  币种选择器；P0 不新增 `mdm_currency`、币种主数据任务或 currency-id provider。该阶段级增量合同解除
  仅因币种缺口产生的 API/DDL 与 confirm wiring 冻结，但不自动授予 V101/V102、共享 writer、门户、验收或合并权限。
- BM-063 发现既有 `AccountSetApplicationService.enabledOrProvisionBaseline(...)` 会在无账套时产生写入，
  不能用于只读列表/详情；BM-063/BM-067 还需要 active `sys_user.id` 与 employee/org/dept 的受控映射。
  用户已批准建立一个左侧“项目”分组内、未置顶的 S6 system-center 公共支撑任务，唯一 owner 串行增加
  纯只读 `AccountSetApplicationService.findEnabled(...)` 和 `SystemAccessService.activeUserIdentities(...)`。
  候选 `630e26cd4854c7d23ebff25b055548dff88b17a5` 已按 `task → feat → group → stage` 正常分层合入
  `stage/s6@fbc8a6faf6efe06959a25fbe6282482d6e355423`；累计 Stage 上直接回归 8/8 通过，故
  `DEP-S6-045`～`047 / HARD_RUNTIME / IMPLEMENT / RESOLVED`。该实现未新增表、迁移、Controller、权限码、
  UI 或账套选择语义，也未扩张到 master-data、settlement、common-platform、V101/V102 或门户共享路径。
  BM-063/BM-067 仍须由阶段总控从最新 clean Stage fixed point 另行调度消费，禁止向旧 formal 反向合并 Stage；
  其余未关闭 provider 与 `fail-closed` 约束不因本次局部解阻而解除。
- BM-067（采购结算单）曾在已批准后发现使用模块私有 session/bootstrap/navigation（会话/引导/导航）伪造完整门户。
  当前已保留业务内容设计与已有独立工作，在真实统一门户完成一致性修正，并由用户明确批准完整页面 UI 复验；
  已登记 `MODULE_UI_APPROVAL_INDEPENDENT=APPROVED` 与 `PROTOTYPE_PORTAL_CONTEXT_PARITY=PASS`，此前
  `PORTAL_REAPPROVAL_REQUESTED` 状态和 PortalShell/router/menu 局部冻结作为历史审计链保留，但已不再生效。
  BM-067 继续沿用原 formal 工作树与 ancestry 开发，不回滚、不重建；V100 与共享门户接入已经稳定并释放租约，
  后续只继续模块私有收口，若再次修改共享资源必须重新申请；
  正式 UI 忠实度、真实功能验收、分层合并和 `VERIFIED` 仍须分别满足原门禁，不因本次 UI 复批自动通过。
- BM-069（核销单）完整已登录页面与业务内容已由用户明确批准，登记
  `MODULE_UI_APPROVAL_INDEPENDENT=APPROVED` 与 `PROTOTYPE_PORTAL_CONTEXT_PARITY=PASS`。UI 门禁已解除，
  但 `DEP-S6-022 settlement-center → BM-069` 仍为 `HARD_RUNTIME / IMPLEMENT / OPEN`，所以当前禁止创建或启动 formal；
  待该依赖关闭后才从届时最新 clean `stage/s6` 创建。`DEP-S6-023`～`032` 继续阻断真实验收，UI 批准不等于
  真实功能验收、合并或 `VERIFIED`。
- 迁移号、原型端口和公共验收端口均读取当前 S6 计划和阶段总控租约。计划形成时已发布迁移到 V98，S6 从当时租用的 V99+
  追加；该数字只是阶段事实快照，不得固化成未来阶段通用规则。

## 16. 明确退役且不得恢复的规则

以下规则已于 2026-08-11 从活动开发树退役，不得作为开发、测试、验收、合并或
`VERIFIED`（已经验证状态） 的前置：

- R0-D（已退役的第零阶段第四部分审计流程） 12h（十二小时）、7 checkpoint（检查点）、Attempt（审计尝试） 3 等审计流程；
- G0（第零门禁）～G4（第四门禁） 活动开发门禁；
- 定时巡检、4h（四小时） STALE（停滞）、5h（五小时） ESCALATED（已升级处理） 等旧时效治理；
- C+E（代码提交加证据子提交） evidence-only（仅证据） 子提交及其 GateResult（门禁结果） 证据链；
- 双轴审查、定时审查和重复 review（审查）；
- 模块开发期间反复执行全仓门禁；
- 全局 UX（用户体验） baseline（基线）、参考页注册表和 UXChangeRequest（用户体验变更申请）；
- 旧隔离阶段、旧 R0（第零阶段） 脚本、旧数据库演练脚本和旧治理 CI（持续集成）；
- 以旧截图、旧报告或旧 PASS（通过） JSON（结构化数据格式） 替代真实运行和用户验收。

恢复任何退役规则必须由用户重新裁定，并同步修改最新 Stage（阶段） tree（版本控制目录树） 的 `AGENTS.md`（智能体开发规则文件）、
`p0-control.yml`（项目唯一控制文件）、Schema（结构约束）、轻量测试和人工审阅副本；仅恢复一份旧文件无效。

## 17. 模块完成检查表

以下用于完整业务模块生命周期；仅测试夹具技术续作按第 12.3 节的专项完成标准执行，不增加不相关验收。
模块任务须满足全部适用项后才算完成。新增角色项按第 9.5 节衔接，不因本次文档合并追溯重验已完成模块，
也不把尚未裁定的组织细节变成新的业务验收门禁：

- [ ] 唯一可见开发任务位于左侧“项目”下，原型、正式开发及反馈修复未另建第二开发任务；独立可见测试任务清楚关联原开发任务且不是第二代码负责人。
- [ ] 当前 rolling batch（滚动批次） 已有有效 business-link plan（业务联动规划）。
- [ ] 自 S6（第六阶段）起，本阶段母版已获用户完整 UI 批准且阶段 UI/能力复用基线已激活；母版批准前未派发后续模块 UI 原型。
- [ ] 任务 owner（负责人）、Group（分组） 并发、共享资源和 Flyway（数据库迁移工具） 窗口不冲突。
- [ ] PREPARE（只读预读准备） 为 READY（准备就绪）；文档、manifest（清单）、分支、HEAD（当前提交头）/tree（版本控制目录树） 和依赖已核对。
- [ ] 有 UI（用户界面）/新交互时，prototype（交互原型） 已完成并由用户明确批准。
- [ ] 自 S6（第六阶段）起，模块已读取活动复用基线并报告全部复用项、新增项、偏离项；零差异已明确报告，且仍由用户查看完整原型并明确批准。
- [ ] 存在界面原型时，原型完整复用公共门户；自 S5（第五阶段）起使用单一多路网关，模块独立批准且未占用真实验收窗口。
- [ ] 自 S6（第六阶段）起，`PROTOTYPE_PORTAL_CONTEXT_PARITY` 已通过：tenant/company/user、门户能力、完整导航和全局入口与本阶段唯一指纹一致；未用拦截、Service Worker、fixture 或硬编码替换全局门户事实。
- [ ] formal（正式开发） task（任务分支） 从调度时最新 clean（干净无未提交变更） Stage（阶段） fixed point（固定基点） 创建。
- [ ] prototype（交互原型） 未进入 formal（正式开发） ancestry（祖先链）。
- [ ] 真实前端、后端、数据库和 API（应用程序接口） 按范围完成。
- [ ] 当前模块及直接上下游必要测试通过。
- [ ] 对适用的界面模块，获批原型提交与目录树已映射到正式组件和真实接口；指定参考页模式已复用，偏离部分已有用户重新批准。
- [ ] formal（正式开发）复用产品共享实现并忠实实现获批原型，未把原型仅作对照后重新设计或重新生成页面。
- [ ] 对适用的界面模块，在向用户发起真实验收请求或邀请前，已固定桌面视口逐屏核对并通过六类最小视觉回归；核对记录留在原模块任务。
- [ ] 原开发任务已交付本次完整测试流程、已批准需求及原型、精确候选、入口和必要前置；测试不重复开发侧全部自动化自测，凭据不在协调消息中明文传递。
- [ ] 对适用的界面或交互模块，独立可见测试任务已使用 computer-use（计算机操作技能）在精确部署候选上完成冻结范围内的真实页面操作；未强制开发和测试各跑一遍相同完整检查。
- [ ] 测试问题回原开发任务核实修复，复验复用原测试任务，完整记录留在测试任务；测试已向人工报告实际结论和未覆盖项。
- [ ] 创建、编辑、选择、保存、提交、状态推进及适用的导入、导出、下载、打印、上传、附件和反向动作均已核对最终结果；没有仅凭按钮提示、接口响应或测试通过宣称页面完成。
- [ ] 本模块独立验收范围包含且已具备 provider 的跨页面/跨模块链路，已从上游页面操作到下游并核对最终回写及刷新持久化；阶段末才执行的完整链路明确标记 `DEFERRED`，未被提前写成通过。
- [ ] 真实验收环境身份达到 IDENTITY_READY（验收身份准备就绪）；自 S5（第五阶段）起已按队列取得公共窗口，例外环境有对应裁定。
- [ ] 运行正式任务提交与目录树、阶段基点和迁移版本已核对，切换数据已安全重置，登录、公共门户、菜单和路由就绪。
- [ ] 报告“等待 UI 验收”或“等待真实验收”前已执行 `USER_REVIEW_ENTRY_LOGIN_READY`：优先自动登录并停留目标路由；不能自动登录时已在直接面向用户的最终汇报中提供临时非生产账号、密码和地址，且凭据未进入仓库、日志或 Agent 协调消息。
- [ ] 适用的独立页面测试通过后，由原开发任务邀请用户真实功能验收，用户已明确通过；未把测试通过当成人工验收通过。
- [ ] 用户提出的正式页面复刻失败已修复并通过忠实度检查，未把仅对原型有效的批准当成正式验收通过。
- [ ] 依赖、provider（能力提供方）、唯一 writer（写入者） 和共享资源状态已回填。
- [ ] 缺陷修复只验证原失败、本次修复及有直接影响依据的最小范围；扩测已说明影响、依据和结束边界，未沿模块关联递归扩测、扩修或新增门禁。
- [ ] 主控未自行打开原型或正式验收页面，只接收调度、协调和最终结果及必要定位；普通修复和测试操作流水未回灌主控。
- [ ] 已按 task（任务分支）→feat（功能分支）→group（分组）→stage（阶段） 正向合并。
- [ ] 模块专属资源已停止；自 S5（第五阶段）起已在分层合入阶段后释放公共验收窗口，未停止仍需使用的共享环境。
- [ ] commit/tree/worktree（提交/目录树/工作树） clean（干净无未提交变更）已核对。
- [ ] 模块任务标题更新为“已验收并合入 Stage（阶段）”并按规则顶置。
- [ ] 未经用户批准未删除分支、工作树、容器、卷或验收材料。

## 18. 阶段完成检查表

新增主副控和独立测试组织项按第 9.5 节衔接；未决权限不自行扩展为阶段新门禁，历史有效结果不因更换执行者重做。

- [ ] 适用新协作规则时，两个副主控已按经裁定的分工承担调度/协调分流；未把旧临时消息整理助手当作两个副主控已建立的证明。
- [ ] 开发任务、测试任务及协调归属清楚；主控只汇总调度、协调和最终结果，未擅自开验收页面或常规接收全量修复上下文。
- [ ] 全项目 28 Group（分组）/85 BM（业务模块）/86 TI（技术项）及当前阶段所属子集映射未发生控制面漂移。
- [ ] business-link plan（业务联动规划） 已按最新累计事实更新。
- [ ] 自 S6（第六阶段）起，阶段 UI/能力复用基线、用户批准的新增共享项及各模块完整视觉批准状态已更新；模块私有项未被误登记为共享资产。
- [ ] 自 S6（第六阶段）起，唯一门户上下文、标准投影/共享校验器及指纹已由阶段总控维护；全部模块未各自接受不同身份、导航或哈希。
- [ ] 硬依赖无环，未关闭项有精确 owner（负责人） 和处置。
- [ ] 当前阶段唯一累计 Stage（阶段） tree（版本控制目录树） clean（干净无未提交变更）。
- [ ] 自 S5（第五阶段）起，累计目录树在一套公共环境完成干净重建、全新数据库完整迁移及固定地址全量功能验收。
- [ ] 适用模块的正式界面忠实度及用户真实验收结果已核对，未将未修复的复刻失败带入阶段完成结论。
- [ ] 适用模块的独立页面测试最终结论及原测试任务记录定位已核对，关键业务动作、文件结果、刷新持久化及范围内跨模块回写有实际结果；
  未以接口或自动化测试代替页面操作，未为阶段汇报重复执行模块完整测试或把全部操作日志回灌主控。
- [ ] 模块期保持 `DEFERRED` 的跨模块联动、完整业务链、E2E、累计验收和全局 UI 清理已在唯一最新 clean Stage tree 上集中完成并通过；若存在修复，复验边界、适用授权及未受影响结论的保留均已核对。
- [ ] 诊断性盘点结果未被用作最终通过证据，待落地提速方案未被写成已实现能力。
- [ ] 上一阶段 provider（能力提供方） 被真实消费，fail-closed（失败时安全拒绝） 场景有效。
- [ ] 唯一事实 writer（写入者）、引用保护和共享资源顺序正确。
- [ ] 下一阶段 provider（能力提供方） 目录已登记。
- [ ] 阶段验收和共享资源已统一停止并保留，最终提交及工作树状态已核对。
- [ ] 用户已看到剩余问题、未关闭依赖和 promotion（晋级） 风险。
- [ ] 未经用户明确批准，未进入 integration/p0（第一期集成分支）、release/p0（第一期发布分支） 或 main（主分支）。

## 19. 事实来源回执

### 19.1 2026-09-12 文档合并来源与变更边界

本次只读取并合并用户指定的两份文档，更新本文件中主流程、角色分工、任务可见性、页面权限、测试修复闭环、
汇报方式、状态和检查表。两份来源文件未修改，未同步仓库机器规则，未创建任务、打开验收页面、操作容器或运行业务测试。

| 来源 | 纳入内容与定位 |
| --- | --- |
| [2026-09-11-独立可见测试任务与原开发负责人修复规则-待调整草案.md](D:/工作/开发文档/人工裁定新增规则文档/2026-09-11-独立可见测试任务与原开发负责人修复规则-待调整草案.md) | 独立左侧测试任务、唯一开发负责人、完整交接、真实页面测试、原任务修复与最小复验、原开发邀请人工验收；对应第 2、4、7.2、9.4、10、14、17、18 节 |
| [2026-09-11-主副控调度与独立页面测试协作规则-待人工裁定草案.md](D:/工作/开发文档/人工裁定新增规则文档/2026-09-11-主副控调度与独立页面测试协作规则-待人工裁定草案.md) | 主控页面禁行、消息收敛、两个副主控、开发/测试各自开页与协作；对应第 4、9.4、10、17、18 节；未定细节集中第 9.5 节 |

两份材料中相同要求合并一次；第二份草案曾列待裁定、但第一份已经明确的“测试任务可见、复验沿用原测试任务、
原开发负责验收邀请”等事项采用第一份的明确描述，不重复列为未决。两个副主控具体组织方式等真正未决内容仍保留。

本次仅做文档直接核对：覆盖两份来源的明确条款、替换相冲突旧表述、同步交叉引用与检查表、保留待裁定项及历史事实边界。
下面的仓库哈希、测试数量和通过回执均为旧记录，不是 2026-09-12 的新运行结果。

### 19.2 2026-09-10 历史仓库事实回执（本次未重新验证）

2026-09-10 的同步记录：BM-061 API 合同补充裁决、既有 V99 共享 writer 复用纠偏、账户派生币种合同批准状态，
以及 BM-063/BM-067 system-center 公共只读支撑的调度、最小业务实现、分层提升与依赖关闭，
已在下列 S6 阶段提交中持久化；未改写 P0.4-R4 不可变快照，也不据此宣布任何模块或
S6（第六阶段）验收完成。
当时机器事实来自干净的 `D:\工作\项目-worktrees\s6-stage-controller` 中 `stage/s6`；旧任务工作树、旧分支标题或旧外部副本
均不得覆盖该来源。

| 来源 | 绑定值 |
| --- | --- |
| 历史核验日期 | 2026-09-10 20:44:59 +08:00 |
| Source ref（来源引用） | `stage/s6`（第六阶段分支） |
| Source HEAD（来源提交头） | `f444445ba2d9d2d2107f71c20a24e72f105516da` |
| Source tree（来源目录树） | `f130bd996d08e42abc65f47eb79418474be73d04` |
| `p0-control.yml`（项目唯一控制文件） blob（版本控制文件对象） | `55a78b7e11e126a5d3e6c6079d9b348de74af0f9` |
| `p0-control.yml` 当前文件 SHA-256 | `d8b3839ea379ff2b5a416185940b98822d2ceccfe33c549a9069fd7c06ecafcd` |
| `AGENTS.md`（智能体开发规则文件） blob（版本控制文件对象） | `b3f3f67aeb9756669b07414bc5e98fa0ad639f3e` |
| `docs/governance/schema/p0-control.schema.json`（控制文件结构约束） blob（版本控制文件对象） | `201bd3202bfdfe2cce86b1f1e61807348747edef` |
| Stage Controller（阶段总控）编排规则 blob（版本控制文件对象） | `0b5de76767a07d49998fd4afd31682fdbb89328f` |
| 阶段 UI/能力复用基线模板 blob（版本控制文件对象） | `cc89dc3064f0894f4256ac7a311d611cfaa7b1f6` |
| S6 business-link plan（业务联动规划）人工文件 blob（版本控制文件对象） | `12c33b53838135212240999fba48027915c875f7` |
| S6 business-link plan（业务联动规划）机器文件 blob（版本控制文件对象） | `b8bd1a3154692651bc7606158c5ead065ca75266` |
| S6 UI/能力复用基线 blob（版本控制文件对象） | `8c642c3d9bb78ca04b82303575ea52f6a1ad0b50` |
| S6 统一门户标准投影 blob（版本控制文件对象） | `d10534327c5b57bc8afcd0ef3ef1e514710fc42c` |
| BM-067 UI 复用与纠偏报告 blob（版本控制文件对象） | `90db36bd500220aab3e86c63a2499e509fedf1b4` |
| BM-069 UI 复用与调度报告 blob（版本控制文件对象） | `427fcadc890e28ce3ab4feaec636af347f6153bb` |
| BM-063 UI 复用与调度报告 blob（版本控制文件对象） | `8c992ff823e46053a336fa747286fed4d15175d6` |
| BM-061 UI 复用与调度报告 blob（版本控制文件对象） | `4ae96a2c65cd5229bc1d661b90babd1ffa60ecbf` |
| `scripts/tests/test_current_development_flow.py`（现行开发流程测试源码）blob（版本控制文件对象） | `fcc14d15d36ca21a1639328f48342cfbe2ce2993` |
| 两段开发与复用规则提交 | `5d3a5d9984b3566cf38d5ed1ff2fae0fe9d6a5c0` |
| S6 业务联动计划提交 | `da434c07d955e132807fd9eb5aa48e662ea59430` |
| S6 母版复用基线激活提交 | `1662acb4dec76f64a3b4f851d32a4211d4064bbc` |
| 验收入口登录就绪规则提交 | `2e0ba73b73c92aa3168294ec4fbb0735e83d7e3c` |
| 统一门户上下文与 BM-067 纠偏提交 | `9c96c4e216235837a37d5a62c9b46ad2ac74f4ab` |
| S6 联动计划控制哈希绑定刷新提交 | `3216a24a5f02ac3d1816823d08ae4b79a99359e1` |
| BM-067 完整页面 UI 复批登记提交 | `2247c6c96e35a2cb7f7e05ed66163eaf1414c40f` |
| BM-069 完整页面 UI 批准登记提交 | `61e36b7ca6ea3513b91f8bb36174730f58791804` |
| BM-063 完整页面 UI 批准登记提交 | `a7ca648f521c163aa701200ed5be618d9711a5a9` |
| BM-061 完整页面 UI 批准登记提交 | `817462fdf61cc61d126dd49fd4324564d6018b28` |
| BM-061 API 合同补充裁决提交 | `90828e533be04049dbc623938190178ebac80612` |
| S6 共享账户 writer 与 V101/V102 串行调度提交 | `1def1c32b415c9a7324ec8131d38b000502dd71f` |
| S6 既有 V99 共享 writer 复用纠偏提交 | `551de48b5042e9cbc2416d1ae57aebfc5b6b6d9f` |
| S6 system-center 公共支撑调度提交 | `abb4759424bcf8e95fecb98f731feeaf865d268d` |
| S6 system-center 公共支撑 task 候选提交 | `630e26cd4854c7d23ebff25b055548dff88b17a5` |
| S6 system-center 公共支撑合入 Stage 提交 | `fbc8a6faf6efe06959a25fbe6282482d6e355423` |
| S6 system-center 公共支撑依赖关闭提交 | `f444445ba2d9d2d2107f71c20a24e72f105516da` |
| 最小修复/最小验证人工裁定来源 SHA-256 | `9a09d6587f8fe008ef0df0a8e728381581e12c8df693a49a6a08d15c39c0d112`；来源文档自述整理日期为 2026-09-11，规则已由用户确认但尚未同步仓库机器控制 |
| R0-D（已退役的第零阶段第四部分审计流程）/旧门禁退役提交 | `00ce62752cca755962f19550dc9251d041502c66` |
| 不可变 manifest（清单）SHA-256（安全散列算法二百五十六位） | `a4e72bec1ea3f4c51802fd7ddd95030230299d81a0a414de126f3c3be8100e98` |
| 八月十一日已生效人工联动规则 SHA-256（安全散列算法二百五十六位） | `039e69e655287e1b7d169a2b9b1a64f9fb5cb5bd68e4f311e3e43c338f4b4319` |
| 九月一日批量盘点与并行收口人工裁定 SHA-256（安全散列算法二百五十六位） | `47833def701b90928752b7468c83c1ba5feb90d2e39c4ce8db9b7fd93f41da46` |
| 九月一日测试基础设施提速人工裁定输入 SHA-256（安全散列算法二百五十六位；仅绑定来源，不证明落地） | `a95fc0b5c24731a19d648743f9842a95be6ec30db11a386667ac67fe49f8a4a3` |

2026-09-10 历史实际执行结果（以下结果本次未重跑）：

- P0.4-R4 不可变快照校验通过，共 209 个文件。
- `p0-control.yml` 与 Schema 检查通过：7 个 Stage、28 个 Group、85 个 BM、86 个 TI、15 个公共文档、18 个导航索引、
  11 个代码模块、7 个公共能力、3 条业务链。
- `python -m unittest scripts.tests.test_p0_contracts scripts.tests.test_current_development_flow` 通过，共 35 项测试。
- 当前 `stage/s6` 工作树 clean，`git diff --check` 通过。
- 初次核对发现 S6 计划内 `controlHash` 仍为旧值
  `392bc90987c560203fee9146249d55eba8ca72c473259c05408a9867f7d9fc84`，而当前控制文件 SHA-256 已为
  `d8b3839ea379ff2b5a416185940b98822d2ceccfe33c549a9069fd7c06ecafcd`，检查曾返回
  `BLOCKED: business link plan control SHA-256 mismatch`。随后由阶段总控提交 `3216a24a` 仅刷新该绑定并保留原范围、依赖、波次和资源裁定；
  复验通过：S6 共 13 个模块、44 条依赖、4 个波次。

2026-09-10 的上述核验未运行后端、前端、数据库或业务验收测试；2026-09-12 本次也未运行这些测试或重跑治理校验。
不把轻量治理校验、测试源码存在或本 SOP 更新当成任何模块的
正式 UI 忠实度、真实可操作性、用户验收或阶段完成已经通过。

后续 Stage（阶段） tree（版本控制目录树）、人工裁定或 manifest（清单） 变化后，应重新生成本 SOP（标准作业程序） 的事实绑定，不得把本文件
当成永远不变的第二套机器控制面。

## 20. 累计修订记录与来源定位

初版于 2026-08-31 生成，绑定 `stage/s4@2e2c7a72c565f4676710e752cf58a73ec64d6f75`（初版第四阶段事实基线）；
上一版于 2026-09-07 同步到 `stage/s5@e36767cb48813c814a503fd7036a7a68ac2237dd`（上一版第五阶段事实基线）。
2026-09-08 版随后绑定 `stage/s5@d4cb0292e730dea0da8b3cbe11b4429c69045b1e`；2026-09-10
更新到页首所列第六阶段提交。2026-09-12 本次只合并主副控与独立测试协作规则，不刷新仓库事实基线。
文件名保留历史日期，正文更新日期与历史机器事实核验日期分别标注。
六个开发槽位、单模块唯一负责人、分层合并主顺序、用户真实验收要求及旧审计退役状态继续有效。

| 事实日期 | 累计更新内容 | 来源与适用边界 |
| --- | --- | --- |
| 2026-09-01 | 阶段公共真实验收环境、串行窗口、精确部署、数据重置、累计环境重建 | 集成提交 `e55d6434`；适用于第五至第七阶段；对应第 10、12、14、17、18 节 |
| 2026-09-01 | 左侧可见模块任务归属改为“项目” | 集成提交 `3f464c2f`；磁盘隔离工作树模板不因此改变；对应第 4、17 节 |
| 2026-09-02 | 原型并行展示、模块批准独立生效、正式开发并行 | 集成提交 `9b5c0fd8`；原型不占真实验收窗口；对应第 2、6、7 节 |
| 2026-09-02 | 一个轻量原型网关、一个外部端口、多模块独立地址 | 集成提交 `33fec2af`；完整公共门户复用；对应第 6.2、6.4 节 |
| 2026-09-01 及后续裁定 | 夹具批量诊断、原负责人修复、具体授权下的验证恢复 | 第四阶段计划及外部批量盘点裁定；不把历史断点续跑扩成无条件许可；对应第 12.3、12.4 节 |
| 2026-09-03 | 第五阶段导出请求、选择范围、异步执行与文件结果统一约束 | 提交 `032b4334`、`4e48bc2a`；仅限已启用导出模块；对应第 9.1 节 |
| 2026-09-05 | 保留已有审查结论，增量复验；公共依赖最小补齐、局部阻断 | 第五阶段计划中的用户纠正；不恢复双轴机器门禁；对应第 3.5、9.2 节 |
| 2026-09-01 记录，2026-09-08 核对 | 测试容器及应用上下文提速保持“未证明落地” | 外部人工裁定输入；本次新增控制变化未登记该提速专项；对应第 12.5 节 |
| 2026-09-07 21:40 | 正式界面忠实度：视觉合同映射、固定视口逐屏核对、参考模式复用、六类回归及失败修复 | 集成提交 `66338988`；机器适用于第五至第七阶段；对应第 2、6.5、7.1、9、10、14、17、18 节 |
| 2026-09-08 18:40:52 +08:00 | 新增 computer-use 真实页面操作检查：逐项页面操作、真实副作用、刷新持久化、文件结果、失败路径及跨页闭环 | 已同步根规则、机器控制文件、Schema 和轻量测试；适用于第五至第七阶段；对应第 2、7.2、9、10、14、17、18 节 |
| 2026-09-07～2026-09-08 | 第五阶段迁移排号调整、车销退单局部验收范围、自提退单打印最小租约 | 当前阶段计划追加记录，属于具体执行范围，不改通用主流程；对应第 15.1 节 |
| 2026-09-09 18:35:54 +08:00 | 两段开发与复用：先阶段母版和复用基线，再逐模块完整 UI 批准；按获批原型独立实现，阶段末集中联动 | 提交 `5d3a5d9984b3566cf38d5ed1ff2fae0fe9d6a5c0`；自 S6 起适用于全部后续阶段和模块；对应第 2、6、7、12、17、18 节 |
| 2026-09-09 18:53:34 +08:00 | S6 结算业务联动规划、formal 波次、唯一 writer、共享资源及阶段末两条结算链 | 提交 `da434c07d955e132807fd9eb5aa48e662ea59430`；属于 S6 当前计划；对应第 3、8、12、15.2 节 |
| 2026-09-10 10:11:23 +08:00 | BM-059 母版批准后激活 S6 UI/能力复用基线 | 提交 `1662acb4dec76f64a3b4f851d32a4211d4064bbc`；后续模块仍须逐一完整视觉批准；对应第 6、15.2、17、18 节 |
| 2026-09-10 10:58:58 +08:00 | UI/真实验收汇报前自动登录目标页面，失败时直接提供临时非生产凭据 | 提交 `2e0ba73b73c92aa3168294ec4fbb0735e83d7e3c`；对应第 6、10、17 节 |
| 2026-09-10 12:53:19 +08:00 | 统一门户上下文、可复算指纹、禁止全局门户 mock、批准后发现偏离的局部冻结与整页复批 | 提交 `9c96c4e216235837a37d5a62c9b46ad2ac74f4ab`；自 S6 起适用；对应第 6.7、10、14、15.2、17、18 节 |
| 2026-09-10 15:24:49 +08:00 | 刷新 S6 联动计划的当前控制文件 SHA 绑定并解除计划检查阻塞 | 提交 `3216a24a5f02ac3d1816823d08ae4b79a99359e1`；不改变范围、依赖、波次或共享资源裁定；对应第 19 节 |
| 2026-09-10 15:46:08 +08:00 | BM-067 完整页面 UI 复批通过，解除门户局部冻结并恢复原 formal ancestry 开发 | 提交 `2247c6c96e35a2cb7f7e05ed66163eaf1414c40f`；历史纠偏状态保留，真实验收与 `VERIFIED` 未自动通过；对应第 6.7、15.2、19 节 |
| 2026-09-10 17:02:03 +08:00 | BM-069 完整页面 UI 批准通过，但实现期运行时硬依赖继续阻断 formal | 提交 `61e36b7ca6ea3513b91f8bb36174730f58791804`；等待 `DEP-S6-022` 关闭，不创建 formal；对应第 8、15.2、19 节 |
| 2026-09-10 17:12:40 +08:00 | BM-063 完整页面 UI 批准通过，解决实现期依赖后派发独立 formal | 提交 `a7ca648f521c163aa701200ed5be618d9711a5a9`；V101 等待前序 Flyway 租约释放；对应第 8、15.2、19 节 |
| 2026-09-10 17:29:55 +08:00 | BM-061 完整页面 UI 批准通过，解决实现期依赖后派发独立 formal | 提交 `817462fdf61cc61d126dd49fd4324564d6018b28`；V102 顺排等待 V100/V101；对应第 8、15.2、19 节 |
| 2026-09-10 17:34:21 +08:00 | BM-067 释放 V100 与 PortalShell/router 租约，Flyway 队列推进至 BM-063 | V101 成为队首但仍须在精确写入点明确授予；V102 继续等待 V101；对应第 8、15.2 节 |
| 2026-09-10 17:46:35 +08:00 | 批准 BM-061 API 合同补充裁决，补齐列表、新建、独立追加备注、复制和批量确认 | 提交 `90828e533be04049dbc623938190178ebac80612`；复用既有权限，新建/复制/批量确认幂等；不改冻结快照或 I/O 禁令，不授予迁移与共享资源租约；对应第 8、15.2、19 节 |
| 2026-09-10 17:54:38 +08:00 | 首次尝试单一调度 S6 共享账户 writer，并保持 V101→V102 串行 | 提交 `1def1c32b415c9a7324ec8131d38b000502dd71f`；后续发现遗漏 BM-059/C 的既有 V99 候选，owner 结论已由 `551de48b` 前向纠正，不再作为当前资源归属；对应第 15.2、19 节 |
| 2026-09-10 18:11:19 +08:00 | 识别并保留 BM-059/V99 既有共享 writer，撤销 BM-061/BM-063 重复建表安排，登记共同币种合同缺口 | 提交 `551de48b5042e9cbc2416d1ae57aebfc5b6b6d9f`；V99 是 BUILD_ONCE 候选，V101/V102 仍预留未授予，`S6-CURRENCY-01` 待用户裁决；对应第 8、15.2、19 节 |
| 2026-09-10 19:20:55 +08:00 | 用户批准 S6 账户派生币种合同，解除 BM-061/BM-063 的币种口径冻结 | 提交 `659db96827cbed682639dda29ada80654164e647`；接口不接收 `currencyId`，服务端从启用资金账户取得并复校 `currencyCode`，多账户同币种，单据保存 `currency_code` 快照，P0 不新增 `mdm_currency`；V101/V102 与共享资源仍须单独取得租约；对应第 15.2、19 节 |
| 2026-09-10 20:03:33 +08:00 | 用户批准 S6 system-center 公共只读支撑任务并登记 BM-063/BM-067 实现期依赖 | 提交 `abb4759424bcf8e95fecb98f731feeaf865d268d`；新增 `DEP-S6-045`～`047`，由单一可见 owner 串行实现账套纯读查询与 active 用户身份映射；不扩张到迁移、UI 或其他共享域；对应第 15.2、19 节 |
| 2026-09-10 20:44:59 +08:00 | S6 system-center 公共只读支撑核证通过并正常分层合入 Stage | task 候选 `630e26cd4854c7d23ebff25b055548dff88b17a5` 经 feat、group 合入 `stage/s6@fbc8a6faf6efe06959a25fbe6282482d6e355423`；累计 Stage 直接回归 8/8、治理 35/35 通过；`f444445ba2d9d2d2107f71c20a24e72f105516da` 将 `DEP-S6-045`～`047` 关闭；对应第 15.2、19 节 |
| 来源文档自述 2026-09-11 | 最小修复、最小验证、禁止门禁偏移 | 用户已确认的人工边界，当前仅外部文档持久化，尚未同步仓库机器控制；对应第 9.3、17、19 节 |
| 2026-09-12（合并 2026-09-11 来源） | 主控不自行开验收页面、只收调度/协调/最终结果；必须建立两个副主控；开发与测试各有对应开页权限 | 按本次用户要求并入本文，不表示仓库规则已接入；副主控具体组织与权限等见第 9.5 节待裁定项；对应第 4、9、10、17、18 节 |
| 2026-09-12（合并 2026-09-11 来源） | 独立可见测试任务接手既有页面操作检查，原开发修复，原测试任务最小复验，原开发邀请用户验收 | 替换禁止独立测试任务及原开发独自完整执行页面检查的旧表述，不新增第二代码负责人或重复门禁，不追溯重验；对应第 2、4、7.2、9、10、14、17、18 节 |

2026-09-10 历史仓库核验读取了当时绑定提交中的以下路径（本次未重新读取）：

- `AGENTS.md`（根开发规则文件）、`p0-control.yml`（项目唯一控制文件）。
- `docs/governance/schema/p0-control.schema.json`（控制文件结构约束）、`scripts/tests/test_current_development_flow.py`（现行流程直接测试源码）。
- `docs/p0/stages/stage-controller-agent-orchestration.md`（阶段总控与模块开发操作约定）。
- `docs/p0/stages/s4/business-link-plan.md`（第四阶段业务联动规划及收尾裁定）。
- `docs/p0/stages/s5/business-link-plan.md`（第五阶段业务联动规划及执行纠正）。
- `docs/p0/stages/s5/business-link-plan.json`（第五阶段机器联动计划）。
- `docs/p0/stages/s5/export-contract-and-unified-remediation.md`（第五阶段导出契约与统一修复规范）。
- `docs/p0/stages/ui-reuse-baseline-template.md`（阶段 UI/能力复用基线模板）。
- `docs/p0/stages/s6/business-link-plan.md/.json`（第六阶段业务联动规划人工与机器文件）。
- `docs/p0/stages/s6/ui-reuse-baseline.md`（第六阶段已激活 UI/能力复用基线）。
- `docs/p0/stages/s6/portal-navigation-canonical.json`（第六阶段统一门户机器可读标准投影）。
- `docs/p0/stages/s6/bm-067-ui-reuse-report.md`（采购结算单复用与门户纠偏报告）。

补充人工审阅来源与本文件位于同一外部目录：

- `2026-09-01-P0.4-R4-阶段最终验证批量盘点与并行收口人工裁定规则.md`（九月一日阶段批量诊断与收尾裁定文档）。
- `2026-09-01-P0.4-R4-MySQL-Testcontainers与Spring上下文复用提速人工裁定规则.md`（九月一日数据库测试容器与应用上下文提速裁定文档）。
- `最小修复最小验证与禁止门禁偏移-人工裁定提示词.md`（用户已确认的最小修复与验证边界；当前尚未接入仓库机器控制）。
- `2026-09-11-独立可见测试任务与原开发负责人修复规则-待调整草案.md`（本次合并来源，原文件未修改，详见第 19.1 节）。
- `2026-09-11-主副控调度与独立页面测试协作规则-待人工裁定草案.md`（本次合并来源，未决项保留在第 9.5 节）。

这些外部路径只供人工审阅定位，不成为持续集成或开发机器的绝对路径依赖。

